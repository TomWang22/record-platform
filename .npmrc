# Relax fetch timeouts and retries for Docker/Colima builds (flaky VM networking to registry.npmjs.org).
# Prevents ERR_SOCKET_TIMEOUT during pnpm install in CI and local Colima/k3d builds.
fetch-retries=5
fetch-timeout=300000
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
