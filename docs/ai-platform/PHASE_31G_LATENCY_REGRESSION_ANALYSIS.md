# Phase 31G — Latency and Regression Analysis

```text
Phase 31G: NOT RUN (blocked by Phase 31D gate failures)
```

Latency regression script added at `scripts/phase31-latency-regression-analysis.mjs` but not executed in closeout path due to BLOCKED matrix.

Artifacts available from soak summary only (under `/tmp`): `phase31-latency-by-protocol.json`, `phase31-latency-by-case.json`, `phase31-latency-by-gate.json`, `phase31-latency-by-user-class.json`, `phase31-latency-outliers-top20.json`
