# OCH → RP namespace patch (generated; NOT applied)
# Matrix: /Users/tom/record-platform/docs/bundles/OCH_TO_RP_CONVERSION_MATRIX.md
# Staging: /Users/tom/bundle-staging/record-platform-kafka-metallb-tls-reference-20260409
# Review with: git apply --check <this-file>

--- a/.github/workflows/kafka-alignment.yml+++ b/.github/workflows/kafka-alignment.yml@@ -131,10 +131,10 @@         if: steps.live.outputs.enabled == 'true'
         run: |
           set -euo pipefail
-          if kubectl get statefulset kafka -n off-campus-housing-tracker --request-timeout=15s >/dev/null 2>&1; then
+          if kubectl get statefulset kafka -n record-platform --request-timeout=15s >/dev/null 2>&1; then
             make kafka-alignment-suite
           else
-            echo "No statefulset/kafka in off-campus-housing-tracker — alignment suite not exercised (expected on fresh k3s)."
+            echo "No statefulset/kafka in record-platform — alignment suite not exercised (expected on fresh k3s)."
           fi
 
       - name: Upload alignment artifacts

--- a/infra/k8s/base/kafka-ca-exporter/configmap.yaml+++ b/infra/k8s/base/kafka-ca-exporter/configmap.yaml@@ -2,7 +2,7 @@ kind: ConfigMap
 metadata:
   name: kafka-ca-exporter-script
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels:
     app: kafka-ca-exporter
 data:
@@ -16,7 +16,7 @@     from kubernetes import client, config
     from prometheus_client import Counter, Gauge, start_http_server
 
-    NS = os.environ.get("HOUSING_NS", "off-campus-housing-tracker")
+    NS = os.environ.get("HOUSING_NS=record-platform", "record-platform")
     INTERVAL = float(os.environ.get("KAFKA_CA_EXPORTER_INTERVAL", "15"))
 
     rotation = Counter(

--- a/infra/k8s/base/kafka-ca-exporter/deployment.yaml+++ b/infra/k8s/base/kafka-ca-exporter/deployment.yaml@@ -2,7 +2,7 @@ kind: Deployment
 metadata:
   name: kafka-ca-exporter
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels:
     app: kafka-ca-exporter
 spec:
@@ -30,8 +30,8 @@               drop: ["ALL"]
             readOnlyRootFilesystem: false
           env:
-            - name: HOUSING_NS
-              value: off-campus-housing-tracker
+            - name: HOUSING_NS=record-platform
+              value: record-platform
             - name: KAFKA_CA_EXPORTER_INTERVAL
               value: "15"
           command:

--- a/infra/k8s/base/kafka-ca-exporter/rbac.yaml+++ b/infra/k8s/base/kafka-ca-exporter/rbac.yaml@@ -2,7 +2,7 @@ kind: ServiceAccount
 metadata:
   name: kafka-ca-exporter
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels:
     app: kafka-ca-exporter
 ---
@@ -10,7 +10,7 @@ kind: Role
 metadata:
   name: kafka-ca-exporter
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
 rules:
   - apiGroups: [""]
     resources: ["secrets"]
@@ -21,7 +21,7 @@ kind: RoleBinding
 metadata:
   name: kafka-ca-exporter
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
 roleRef:
   apiGroup: rbac.authorization.k8s.io
   kind: Role
@@ -29,4 +29,4 @@ subjects:
   - kind: ServiceAccount
     name: kafka-ca-exporter
-    namespace: off-campus-housing-tracker
+    namespace: record-platform

--- a/infra/k8s/base/kafka-ca-exporter/service.yaml+++ b/infra/k8s/base/kafka-ca-exporter/service.yaml@@ -2,7 +2,7 @@ kind: Service
 metadata:
   name: kafka-ca-exporter
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels:
     app: kafka-ca-exporter
 spec:

--- a/infra/k8s/base/kafka-external/external-service.yaml+++ b/infra/k8s/base/kafka-external/external-service.yaml@@ -9,7 +9,7 @@ kind: Service
 metadata:
   name: kafka-external
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
 spec:
   type: ClusterIP
   ports:
@@ -22,7 +22,7 @@ kind: Endpoints
 metadata:
   name: kafka-external
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
 subsets:
   - addresses:
       - ip: 192.168.64.1

--- a/infra/k8s/base/kafka/deploy.yaml+++ b/infra/k8s/base/kafka/deploy.yaml@@ -2,7 +2,7 @@ kind: Deployment
 metadata:
   name: kafka
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels: { app: kafka }
 spec:
   replicas: 1
@@ -47,10 +47,10 @@             - { name: KAFKA_SSL_ENABLED, value: "true" }
             - { name: KAFKA_BROKER_ID, value: "1" }
             - { name: KAFKA_ZOOKEEPER_CONNECT, value: "zookeeper:2181" }
-            - { name: KAFKA_CLUSTER_ID, value: "off-campus-housing-tracker-kafka-cluster" }
+            - { name: KAFKA_CLUSTER_ID, value: "record-platform-kafka-cluster" }
             # Strict TLS: SSL on 9093 (client-facing), PLAINTEXT on 9092 (inter-broker / optional)
             - { name: KAFKA_LISTENERS, value: "PLAINTEXT://0.0.0.0:9092,SSL://0.0.0.0:9093,PLAINTEXT_HOST://0.0.0.0:29092" }
-            - { name: KAFKA_ADVERTISED_LISTENERS, value: "PLAINTEXT://kafka.off-campus-housing-tracker.svc.cluster.local:9092,SSL://kafka.off-campus-housing-tracker.svc.cluster.local:9093,PLAINTEXT_HOST://kafka.off-campus-housing-tracker.svc.cluster.local:29092" }
+            - { name: KAFKA_ADVERTISED_LISTENERS, value: "PLAINTEXT://kafka.record-platform.svc.cluster.local:9092,SSL://kafka.record-platform.svc.cluster.local:9093,PLAINTEXT_HOST://kafka.record-platform.svc.cluster.local:29092" }
             - { name: KAFKA_LISTENER_SECURITY_PROTOCOL_MAP, value: "PLAINTEXT:PLAINTEXT,SSL:SSL,PLAINTEXT_HOST:PLAINTEXT" }
             - { name: KAFKA_INTER_BROKER_LISTENER_NAME, value: "PLAINTEXT" }
             # Keystore/truststore from kafka-ssl-secret (strict TLS)

--- a/infra/k8s/base/kafka/external-service.yaml+++ b/infra/k8s/base/kafka/external-service.yaml@@ -4,7 +4,7 @@ kind: Service
 metadata:
   name: kafka-external
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
 spec:
   type: ClusterIP
   ports:
@@ -17,7 +17,7 @@ kind: Endpoints
 metadata:
   name: kafka-external
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
 subsets:
   - addresses:
       - ip: 10.0.0.8  # Host machine IP - update if different

--- a/infra/k8s/base/kafka/service.yaml+++ b/infra/k8s/base/kafka/service.yaml@@ -2,7 +2,7 @@ kind: Service
 metadata:
   name: kafka
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels: { app: kafka }
 spec:
   selector: { app: kafka }

--- a/infra/k8s/base/observability/prometheus-rules-kafka-health.yaml+++ b/infra/k8s/base/observability/prometheus-rules-kafka-health.yaml@@ -47,7 +47,7 @@     - name: kafka-tls
       interval: 30s
       rules:
-      # Emitted by kafka-ca-exporter (off-campus-housing-tracker) — scraped via housing-pods job when pod has prometheus.io/* annotations.
+      # Emitted by kafka-ca-exporter (record-platform) — scraped via housing-pods job when pod has prometheus.io/* annotations.
       - alert: KafkaCARotated
         expr: increase(kafka_ca_rotation_detected_total[5m]) > 0
         for: 1m

--- a/infra/k8s/kafka-certs/certificates/kafka-0-cert.yaml+++ b/infra/k8s/kafka-certs/certificates/kafka-0-cert.yaml@@ -3,7 +3,7 @@ kind: Certificate
 metadata:
   name: kafka-0-tls
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
 spec:
   secretName: kafka-0-tls
   duration: 8760h
@@ -12,9 +12,9 @@   dnsNames:
     - kafka-0
     - kafka-0.kafka
-    - kafka-0.kafka.off-campus-housing-tracker.svc
-    - kafka-0.kafka.off-campus-housing-tracker.svc.cluster.local
-    - kafka-0-external.off-campus-housing-tracker.svc.cluster.local
+    - kafka-0.kafka.record-platform.svc
+    - kafka-0.kafka.record-platform.svc.cluster.local
+    - kafka-0-external.record-platform.svc.cluster.local
   usages:
     - digital signature
     - key encipherment

--- a/infra/k8s/kafka-certs/certificates/kafka-1-cert.yaml+++ b/infra/k8s/kafka-certs/certificates/kafka-1-cert.yaml@@ -3,7 +3,7 @@ kind: Certificate
 metadata:
   name: kafka-1-tls
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
 spec:
   secretName: kafka-1-tls
   duration: 8760h
@@ -12,9 +12,9 @@   dnsNames:
     - kafka-1
     - kafka-1.kafka
-    - kafka-1.kafka.off-campus-housing-tracker.svc
-    - kafka-1.kafka.off-campus-housing-tracker.svc.cluster.local
-    - kafka-1-external.off-campus-housing-tracker.svc.cluster.local
+    - kafka-1.kafka.record-platform.svc
+    - kafka-1.kafka.record-platform.svc.cluster.local
+    - kafka-1-external.record-platform.svc.cluster.local
   usages:
     - digital signature
     - key encipherment

--- a/infra/k8s/kafka-certs/certificates/kafka-2-cert.yaml+++ b/infra/k8s/kafka-certs/certificates/kafka-2-cert.yaml@@ -3,7 +3,7 @@ kind: Certificate
 metadata:
   name: kafka-2-tls
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
 spec:
   secretName: kafka-2-tls
   duration: 8760h
@@ -12,9 +12,9 @@   dnsNames:
     - kafka-2
     - kafka-2.kafka
-    - kafka-2.kafka.off-campus-housing-tracker.svc
-    - kafka-2.kafka.off-campus-housing-tracker.svc.cluster.local
-    - kafka-2-external.off-campus-housing-tracker.svc.cluster.local
+    - kafka-2.kafka.record-platform.svc
+    - kafka-2.kafka.record-platform.svc.cluster.local
+    - kafka-2-external.record-platform.svc.cluster.local
   usages:
     - digital signature
     - key encipherment

--- a/infra/k8s/kafka-certs/kafka-tls-preflight-job.yaml+++ b/infra/k8s/kafka-certs/kafka-tls-preflight-job.yaml@@ -1,12 +1,12 @@ # Optional: mTLS handshake to each headless broker (INTERNAL :9093).
-# Apply after Kafka + och-kafka-ssl-secret exist:
+# Apply after Kafka + kafka-ssl-secret exist:
 #   kubectl apply -f infra/k8s/kafka-certs/kafka-tls-preflight-job.yaml
-#   kubectl -n off-campus-housing-tracker wait --for=condition=complete job/kafka-tls-preflight --timeout=120s
+#   kubectl -n record-platform wait --for=condition=complete job/kafka-tls-preflight --timeout=120s
 apiVersion: batch/v1
 kind: Job
 metadata:
   name: kafka-tls-preflight
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
 spec:
   backoffLimit: 2
   ttlSecondsAfterFinished: 600
@@ -22,7 +22,7 @@             - |
               set -eu
               apk add --no-cache openssl ca-certificates >/dev/null
-              NS="off-campus-housing-tracker"
+              NS="record-platform"
               fail=0
               for i in 0 1 2; do
                 # Short name + cluster search (ns.svc.cluster.local) — same as app pods resolve.
@@ -49,4 +49,4 @@       volumes:
         - name: client
           secret:
-            secretName: och-kafka-ssl-secret
+            secretName: kafka-ssl-secret

--- a/infra/k8s/kafka-kraft-metallb/external-services.yaml+++ b/infra/k8s/kafka-kraft-metallb/external-services.yaml@@ -5,7 +5,7 @@ kind: Service
 metadata:
   name: kafka-0-external
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels: { app: kafka }
 spec:
   type: LoadBalancer
@@ -20,7 +20,7 @@ kind: Service
 metadata:
   name: kafka-1-external
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels: { app: kafka }
 spec:
   type: LoadBalancer
@@ -35,7 +35,7 @@ kind: Service
 metadata:
   name: kafka-2-external
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels: { app: kafka }
 spec:
   type: LoadBalancer

--- a/infra/k8s/kafka-kraft-metallb/headless-service.yaml+++ b/infra/k8s/kafka-kraft-metallb/headless-service.yaml@@ -2,7 +2,7 @@ kind: Service
 metadata:
   name: kafka
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels: { app: kafka }
 spec:
   clusterIP: None

--- a/infra/k8s/kafka-kraft-metallb/kafka-metallb-alignment-exporter.yaml+++ b/infra/k8s/kafka-kraft-metallb/kafka-metallb-alignment-exporter.yaml@@ -4,13 +4,13 @@ kind: ServiceAccount
 metadata:
   name: kafka-metallb-alignment-exporter
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
 ---
 apiVersion: rbac.authorization.k8s.io/v1
 kind: Role
 metadata:
   name: kafka-metallb-alignment-exporter
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
 rules:
   - apiGroups: [""]
     resources: ["services"]
@@ -26,7 +26,7 @@ kind: RoleBinding
 metadata:
   name: kafka-metallb-alignment-exporter
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
 roleRef:
   apiGroup: rbac.authorization.k8s.io
   kind: Role
@@ -34,13 +34,13 @@ subjects:
   - kind: ServiceAccount
     name: kafka-metallb-alignment-exporter
-    namespace: off-campus-housing-tracker
+    namespace: record-platform
 ---
 apiVersion: apps/v1
 kind: Deployment
 metadata:
   name: kafka-metallb-alignment-exporter
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels: { app: kafka-metallb-alignment-exporter }
 spec:
   replicas: 1
@@ -74,7 +74,7 @@           image: python:3.12-slim-bookworm
           imagePullPolicy: IfNotPresent
           env:
-            - { name: HOUSING_NS, value: off-campus-housing-tracker }
+            - { name: HOUSING_NS=record-platform, value: record-platform }
             - { name: KAFKA_BROKER_REPLICAS, value: "3" }
             - { name: METRICS_PORT, value: "9108" }
             - { name: PYTHONPATH, value: /deps }

--- a/infra/k8s/kafka-kraft-metallb/kafka-pdb.yaml+++ b/infra/k8s/kafka-kraft-metallb/kafka-pdb.yaml@@ -4,7 +4,7 @@ kind: PodDisruptionBudget
 metadata:
   name: kafka-pdb
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels: { app: kafka }
 spec:
   minAvailable: 2

--- a/infra/k8s/kafka-kraft-metallb/kustomization.yaml+++ b/infra/k8s/kafka-kraft-metallb/kustomization.yaml@@ -2,7 +2,7 @@ # Does not replace base/kafka (ZooKeeper single replica); apply this overlay when you want quorum + MetalLB.
 #
 # Prereqs:
-#   - Namespace off-campus-housing-tracker
+#   - Namespace record-platform
 #   - Secret kafka-ssl-secret (scripts/kafka-ssl-from-dev-root.sh — dynamic SANs per replica + optional MetalLB IPs)
 #   - Optional: pnpm verify:kafka-tls-sans — fail if broker PEM SANs drift
 #   - Optional cert-manager: infra/k8s/kafka-certs/README.md
@@ -30,7 +30,7 @@ # Optional Prometheus: kafka-metallb-alignment-exporter compares LB Service IPs to broker advertised EXTERNAL.
 apiVersion: kustomize.config.k8s.io/v1beta1
 kind: Kustomization
-namespace: off-campus-housing-tracker
+namespace: record-platform
 generatorOptions:
   disableNameSuffixHash: true
 configMapGenerator:

--- a/infra/k8s/kafka-kraft-metallb/rbac-kafka-svc-reader.yaml+++ b/infra/k8s/kafka-kraft-metallb/rbac-kafka-svc-reader.yaml@@ -4,14 +4,14 @@ kind: ServiceAccount
 metadata:
   name: kafka
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels: { app: kafka }
 ---
 apiVersion: rbac.authorization.k8s.io/v1
 kind: Role
 metadata:
   name: kafka-lb-svc-reader
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels: { app: kafka }
 rules:
   - apiGroups: [""]
@@ -23,7 +23,7 @@ kind: RoleBinding
 metadata:
   name: kafka-lb-svc-reader
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels: { app: kafka }
 roleRef:
   apiGroup: rbac.authorization.k8s.io
@@ -32,4 +32,4 @@ subjects:
   - kind: ServiceAccount
     name: kafka
-    namespace: off-campus-housing-tracker
+    namespace: record-platform

--- a/infra/k8s/kafka-kraft-metallb/statefulset.yaml+++ b/infra/k8s/kafka-kraft-metallb/statefulset.yaml@@ -9,7 +9,7 @@ kind: StatefulSet
 metadata:
   name: kafka
-  namespace: off-campus-housing-tracker
+  namespace: record-platform
   labels: { app: kafka }
 spec:
   serviceName: kafka
@@ -185,7 +185,7 @@             - name: KAFKA_LISTENERS
               value: "INTERNAL://0.0.0.0:9093,EXTERNAL://0.0.0.0:9094,CONTROLLER://0.0.0.0:9095"
             - name: KAFKA_CONTROLLER_QUORUM_VOTERS
-              value: "0@kafka-0.kafka.off-campus-housing-tracker.svc.cluster.local:9095,1@kafka-1.kafka.off-campus-housing-tracker.svc.cluster.local:9095,2@kafka-2.kafka.off-campus-housing-tracker.svc.cluster.local:9095"
+              value: "0@kafka-0.kafka.record-platform.svc.cluster.local:9095,1@kafka-1.kafka.record-platform.svc.cluster.local:9095,2@kafka-2.kafka.record-platform.svc.cluster.local:9095"
             - { name: KAFKA_LISTENER_SECURITY_PROTOCOL_MAP, value: "INTERNAL:SSL,EXTERNAL:SSL,CONTROLLER:SSL" }
             - { name: KAFKA_INTER_BROKER_LISTENER_NAME, value: "INTERNAL" }
             - { name: KAFKA_CONTROLLER_LISTENER_NAMES, value: "CONTROLLER" }

--- a/infra/k8s/metallb/bgpadvertisement.example.yaml+++ b/infra/k8s/metallb/bgpadvertisement.example.yaml@@ -1,9 +1,9 @@-# BGPAdvertisement: advertise off-campus-housing-tracker-pool via BGP. Apply after BGPPeer.
+# BGPAdvertisement: advertise record-platform-pool via BGP. Apply after BGPPeer.
 apiVersion: metallb.io/v1beta1
 kind: BGPAdvertisement
 metadata:
-  name: off-campus-housing-tracker-pool
+  name: record-platform-pool
   namespace: metallb-system
 spec:
   ipAddressPools:
-  - off-campus-housing-tracker-pool
+  - record-platform-pool

--- a/infra/k8s/metallb/bgpadvertisement.yaml+++ b/infra/k8s/metallb/bgpadvertisement.yaml@@ -1,9 +1,9 @@-# BGPAdvertisement: advertise off-campus-housing-tracker-pool to BGP peer (FRR).
+# BGPAdvertisement: advertise record-platform-pool to BGP peer (FRR).
 apiVersion: metallb.io/v1beta1
 kind: BGPAdvertisement
 metadata:
-  name: off-campus-housing-tracker-pool
+  name: record-platform-pool
   namespace: metallb-system
 spec:
   ipAddressPools:
-    - off-campus-housing-tracker-pool
+    - record-platform-pool

--- a/infra/k8s/metallb/bgppeer-frr.yaml+++ b/infra/k8s/metallb/bgppeer-frr.yaml@@ -2,7 +2,7 @@ apiVersion: metallb.io/v1beta2
 kind: BGPPeer
 metadata:
-  name: off-campus-housing-tracker-frr
+  name: record-platform-frr
   namespace: metallb-system
 spec:
   myASN: 64512

--- a/infra/k8s/metallb/bgppeer.example.yaml+++ b/infra/k8s/metallb/bgppeer.example.yaml@@ -4,7 +4,7 @@ apiVersion: metallb.io/v1beta2
 kind: BGPPeer
 metadata:
-  name: off-campus-housing-tracker-frr
+  name: record-platform-frr
   namespace: metallb-system
 spec:
   myASN: 64512

--- a/infra/k8s/metallb/bgppeer.yaml+++ b/infra/k8s/metallb/bgppeer.yaml@@ -2,7 +2,7 @@ apiVersion: metallb.io/v1beta2
 kind: BGPPeer
 metadata:
-  name: off-campus-housing-tracker-frr
+  name: record-platform-frr
   namespace: metallb-system
 spec:
   myASN: 64512

--- a/infra/k8s/metallb/ipaddresspool.yaml+++ b/infra/k8s/metallb/ipaddresspool.yaml@@ -3,7 +3,7 @@ apiVersion: metallb.io/v1beta1
 kind: IPAddressPool
 metadata:
-  name: off-campus-housing-tracker-pool
+  name: record-platform-pool
   namespace: metallb-system
 spec:
   addresses:

--- a/infra/k8s/metallb/l2advertisement.yaml+++ b/infra/k8s/metallb/l2advertisement.yaml@@ -1,10 +1,10 @@-# L2 advertisement for off-campus-housing-tracker-pool. Optional: set nodeSelector for multi-node priority (see docs/METALLB_TRAFFIC_POLICY_AND_SCALE.md).
+# L2 advertisement for record-platform-pool. Optional: set nodeSelector for multi-node priority (see docs/METALLB_TRAFFIC_POLICY_AND_SCALE.md).
 apiVersion: metallb.io/v1beta1
 kind: L2Advertisement
 metadata:
-  name: off-campus-housing-tracker-l2
+  name: record-platform-l2
   namespace: metallb-system
 spec:
   ipAddressPools:
-    - off-campus-housing-tracker-pool
+    - record-platform-pool
   # nodeSelector: {}   # Uncomment and set to limit which nodes announce L2 (e.g. metallb-priority: high)

--- a/scripts/apply-kafka-kraft-staged.sh+++ b/scripts/apply-kafka-kraft-staged.sh@@ -8,7 +8,7 @@ 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
-NS="${HOUSING_NS:-off-campus-housing-tracker}"
+NS="${HOUSING_NS=record-platform:-record-platform}"
 R="${KAFKA_BROKER_REPLICAS:-3}"
 ATOMIC="${KAFKA_TLS_ATOMIC_BEFORE_REFRESH:-0}"
 

--- a/scripts/check-kafka-config-drift.sh+++ b/scripts/check-kafka-config-drift.sh@@ -4,12 +4,12 @@ #
 # Usage:
 #   ./scripts/check-kafka-config-drift.sh [namespace] [replicas]
-# Env: HOUSING_NS, KAFKA_BROKER_REPLICAS, KAFKA_CLUSTER_EXEC_TIMEOUT
+# Env: HOUSING_NS=record-platform, KAFKA_BROKER_REPLICAS, KAFKA_CLUSTER_EXEC_TIMEOUT
 #
 # Optional: DRIFT_WRITE_PROM_FILE=/path/to/file.prom — append OpenMetrics lines (for node_exporter textfile)
 set -euo pipefail
 
-NS="${1:-${HOUSING_NS:-off-campus-housing-tracker}}"
+NS="${1:-${HOUSING_NS=record-platform:-record-platform}}"
 REPLICAS="${2:-${KAFKA_BROKER_REPLICAS:-3}}"
 EXEC_TO="${KAFKA_CLUSTER_EXEC_TIMEOUT:-45}"
 

--- a/scripts/ci/generate-kafka-ci-tls.sh+++ b/scripts/ci/generate-kafka-ci-tls.sh@@ -25,7 +25,7 @@ # Ephemeral CI CA (7 days)
 openssl genrsa -out "$TMP/ca.key" 2048
 openssl req -x509 -new -nodes -key "$TMP/ca.key" -sha256 -days 7 \
-  -subj "/CN=och-kafka-ci-ca/O=off-campus-housing-ci" \
+  -subj "/CN=och-kafka-ci-ca/O=record-platform-ci" \
   -out "$TMP/ca.pem"
 
 KAFKA_SANS="DNS:kafka,DNS:localhost,DNS:host.docker.internal,IP:127.0.0.1"

--- a/scripts/create-kafka-event-topics-k8s.sh+++ b/scripts/create-kafka-event-topics-k8s.sh@@ -7,7 +7,7 @@ # Prereqs: Stable API tunnel (./scripts/colima-api-health.sh), pods kafka-0..2 Ready.
 #
 # Env:
-#   KAFKA_K8S_NS=off-campus-housing-tracker
+#   KAFKA_K8S_NS=record-platform
 #   KAFKA_K8S_POD=kafka-0
 #   KAFKA_K8S_REPLICATION_FACTOR=3
 #   ENV_PREFIX, PARTITIONS, OCH_KAFKA_TOPIC_SUFFIX — same as create-kafka-event-topics.sh
@@ -18,7 +18,7 @@ REPO_ROOT="${REPO_ROOT:-$(cd "$SCRIPT_DIR/.." && pwd)}"
 export PATH="$SCRIPT_DIR/shims:/opt/homebrew/bin:/usr/local/bin:${PATH:-}"
 
-NS="${KAFKA_K8S_NS:-off-campus-housing-tracker}"
+NS="${KAFKA_K8S_NS:-record-platform}"
 KPOD="${KAFKA_K8S_POD:-kafka-0}"
 RF="${KAFKA_K8S_REPLICATION_FACTOR:-3}"
 ENV_PREFIX="${ENV_PREFIX:-dev}"

--- a/scripts/export-kafka-ca-metric.sh+++ b/scripts/export-kafka-ca-metric.sh@@ -3,10 +3,10 @@ # Does not start an HTTP server — pipe to Pushgateway or redirect to a file if needed.
 #
 # Usage: ./scripts/export-kafka-ca-metric.sh
-# Env: HOUSING_NS (default off-campus-housing-tracker)
+# Env: HOUSING_NS=record-platform (default record-platform)
 set -euo pipefail
 
-NS="${HOUSING_NS:-off-campus-housing-tracker}"
+NS="${HOUSING_NS=record-platform:-record-platform}"
 command -v kubectl >/dev/null 2>&1 || { echo "kubectl required" >&2; exit 1; }
 command -v openssl >/dev/null 2>&1 || { echo "openssl required" >&2; exit 1; }
 

--- a/scripts/kafka-after-rollout-verify-brokers.sh+++ b/scripts/kafka-after-rollout-verify-brokers.sh@@ -4,13 +4,13 @@ # Also scans recent logs for SSL handshake / PKIX failures.
 #
 # Usage:
-#   HOUSING_NS=off-campus-housing-tracker KAFKA_BROKER_REPLICAS=3 ./scripts/kafka-after-rollout-verify-brokers.sh
+#   HOUSING_NS=record-platform=record-platform KAFKA_BROKER_REPLICAS=3 ./scripts/kafka-after-rollout-verify-brokers.sh
 # Env:
 #   KAFKA_TLS_GUARD_SKIP_LOG_SCAN=1 — skip log tail PKIX grep (faster)
 set -euo pipefail
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
-NS="${HOUSING_NS:-off-campus-housing-tracker}"
+NS="${HOUSING_NS=record-platform:-record-platform}"
 REP="${KAFKA_BROKER_REPLICAS:-3}"
 
 echo "▶ Post-rollout: broker Ready + no CrashLoopBackOff (ns=$NS replicas=$REP)…"
@@ -33,5 +33,5 @@ chmod +x "$SCRIPT_DIR/kafka-tls-guard.sh" 2>/dev/null || true
 echo "▶ Cross-broker truststore/keystore parity + PKIX log tail (kafka-tls-guard POST_ROLLOUT_ONLY)…"
 KAFKA_TLS_GUARD_POST_ROLLOUT_ONLY=1 KAFKA_TLS_GUARD_SKIP_VERIFY=1 \
-  HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" \
+  HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" \
   bash "$SCRIPT_DIR/kafka-tls-guard.sh"

--- a/scripts/kafka-auto-heal-inter-broker-tls.sh+++ b/scripts/kafka-auto-heal-inter-broker-tls.sh@@ -3,14 +3,14 @@ # if verification fails or any broker is CrashLoopBackOff, delete all kafka-0..N-1 pods, wait Ready, re-verify.
 #
 # Usage:
-#   HOUSING_NS=off-campus-housing-tracker KAFKA_BROKER_REPLICAS=3 ./scripts/kafka-auto-heal-inter-broker-tls.sh
+#   HOUSING_NS=record-platform=record-platform KAFKA_BROKER_REPLICAS=3 ./scripts/kafka-auto-heal-inter-broker-tls.sh
 # Env:
 #   KAFKA_INTER_BROKER_TLS_HEAL=0 — no-op (exit 0)
 #   KAFKA_INTER_BROKER_TLS_HEAL_MAX_DELETE_ROUNDS — max delete+wait cycles after failed verify (default 1)
 set -euo pipefail
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
-NS="${HOUSING_NS:-off-campus-housing-tracker}"
+NS="${HOUSING_NS=record-platform:-record-platform}"
 REP="${KAFKA_BROKER_REPLICAS:-3}"
 
 if [[ "${KAFKA_INTER_BROKER_TLS_HEAL:-1}" != "1" ]]; then
@@ -26,7 +26,7 @@ chmod +x "$SCRIPT_DIR/kafka-after-rollout-verify-brokers.sh" 2>/dev/null || true
 
 _run_verify() {
-  HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-after-rollout-verify-brokers.sh"
+  HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-after-rollout-verify-brokers.sh"
 }
 
 _any_crashloop() {

--- a/scripts/kafka-clean-slate.sh+++ b/scripts/kafka-clean-slate.sh@@ -2,11 +2,11 @@ # Nuclear reset: StatefulSet + PVCs + Services (corrupted KRaft / cluster.id mismatch / stuck quorum).
 # Destroys broker data — use only when onboarding is broken beyond repair.
 #
-# Env: HOUSING_NS (default off-campus-housing-tracker)
+# Env: HOUSING_NS=record-platform (default record-platform)
 set -euo pipefail
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
-NS="${HOUSING_NS:-off-campus-housing-tracker}"
+NS="${HOUSING_NS=record-platform:-record-platform}"
 
 echo "=== kafka-clean-slate (ns=$NS) — deletes broker StatefulSet and PVCs ==="
 if [[ "${KAFKA_CLEAN_SLATE_CONFIRM:-}" != "YES" ]]; then

--- a/scripts/kafka-onboarding-reset.sh+++ b/scripts/kafka-onboarding-reset.sh@@ -2,10 +2,10 @@ # Remove Kafka LoadBalancer Services + headless Service (and slices) so the next apply-kafka-kraft gets
 # fresh MetalLB allocations and clean headless DNS. Use in dev-onboard before apply-kafka-kraft.
 #
-# Env: HOUSING_NS (default off-campus-housing-tracker)
+# Env: HOUSING_NS=record-platform (default record-platform)
 set -euo pipefail
 
-NS="${HOUSING_NS:-off-campus-housing-tracker}"
+NS="${HOUSING_NS=record-platform:-record-platform}"
 
 echo "=== kafka-onboarding-reset (ns=$NS) ==="
 echo "Deleting kafka-0/1/2-external LoadBalancers and headless kafka Service (recreated by apply-kafka-kraft)..."

--- a/scripts/kafka-quorum-stable.sh+++ b/scripts/kafka-quorum-stable.sh@@ -3,14 +3,14 @@ # Absorbs transient election churn before app rollouts / edge checks.
 #
 # Env:
-#   HOUSING_NS — default off-campus-housing-tracker
+#   HOUSING_NS=record-platform — default record-platform
 #   KAFKA_QUORUM_STABLE_WINDOW_SEC — default 30
 #   KAFKA_QUORUM_STABLE_MAX_WAIT_SEC — default 600 (fail if still churning)
 #   KAFKA_QUORUM_STABLE_POLL_SEC — default 5
 #   KAFKA_QUORUM_STABLE_SKIP=1 — no-op success
 set -euo pipefail
 
-NS="${HOUSING_NS:-off-campus-housing-tracker}"
+NS="${HOUSING_NS=record-platform:-record-platform}"
 WIN="${KAFKA_QUORUM_STABLE_WINDOW_SEC:-30}"
 MAX_WAIT="${KAFKA_QUORUM_STABLE_MAX_WAIT_SEC:-600}"
 POLL="${KAFKA_QUORUM_STABLE_POLL_SEC:-5}"

--- a/scripts/kafka-refresh-tls-from-lb.sh+++ b/scripts/kafka-refresh-tls-from-lb.sh@@ -1,10 +1,10 @@ #!/usr/bin/env bash
-# Regenerate kafka-ssl-secret (and och-kafka-ssl-secret) with SANs that include current
+# Regenerate kafka-ssl-secret (and kafka-ssl-secret) with SANs that include current
 # kafka-*-external LoadBalancer IPs **and** any IPv4 SANs already on the deployed / on-disk
 # broker cert (union). Replacing SANs with only today's LB IPs can drop a still-advertised
 # MetalLB IP after churn and break inter-broker / controller TLS → CrashLoopBackOff.
 #
-# Env: HOUSING_NS, KAFKA_BROKER_REPLICAS, KAFKA_SSL_* (see kafka-ssl-from-dev-root.sh)
+# Env: HOUSING_NS=record-platform, KAFKA_BROKER_REPLICAS, KAFKA_SSL_* (see kafka-ssl-from-dev-root.sh)
 #   KAFKA_SSL_MERGE_EXTRA_IP_SANS — optional comma-separated IPv4s to union in (e.g. stale MetalLB IP
 #   missing from both secret and disk PEM after a bad refresh; use until brokers advertise new IPs).
 #   KAFKA_SSL_AUTO_METALLB_IPS=0 after merge (LB + historical IPs already in EXTRA).
@@ -12,9 +12,9 @@ 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
-NS="${HOUSING_NS:-off-campus-housing-tracker}"
+NS="${HOUSING_NS=record-platform:-record-platform}"
 REP="${KAFKA_BROKER_REPLICAS:-3}"
-export HOUSING_NS="$NS"
+export HOUSING_NS=record-platform="$NS"
 export KAFKA_BROKER_REPLICAS="$REP"
 
 echo "=== kafka-refresh-tls-from-lb (ns=$NS replicas=$REP) ==="

--- a/scripts/kafka-rolling-restart.sh+++ b/scripts/kafka-rolling-restart.sh@@ -3,13 +3,13 @@ # Before each delete: optional app rollout pause, quorum gate (metadata reports a leader).
 #
 # Env:
-#   HOUSING_NS, KAFKA_BROKER_REPLICAS (default 3)
+#   HOUSING_NS=record-platform, KAFKA_BROKER_REPLICAS (default 3)
 #   KAFKA_ROLLING_PAUSE_APPS=1 — rollout pause all Deployments in ns during restart (reduces client storm)
 #   KAFKA_ROLLING_SKIP_VERIFY_STEPS=1 — skip verify-kafka-cluster between pods (faster; less safe)
 set -euo pipefail
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
-NS="${HOUSING_NS:-off-campus-housing-tracker}"
+NS="${HOUSING_NS=record-platform:-record-platform}"
 R="${KAFKA_BROKER_REPLICAS:-3}"
 
 # shellcheck source=/dev/null
@@ -42,7 +42,7 @@   kubectl wait --for=condition=Ready "pod/kafka-$i" -n "$NS" --timeout=300s
   if [[ "${KAFKA_ROLLING_SKIP_VERIFY_STEPS:-0}" != "1" ]]; then
     echo "▶ verify-kafka-cluster after kafka-$i"
-    HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$R" bash "$SCRIPT_DIR/verify-kafka-cluster.sh" "$NS" "$R"
+    HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$R" bash "$SCRIPT_DIR/verify-kafka-cluster.sh" "$NS" "$R"
   fi
 done
 

--- a/scripts/kafka-runtime-sync.sh+++ b/scripts/kafka-runtime-sync.sh@@ -3,7 +3,7 @@ #
 # Usage:
 #   ./scripts/kafka-runtime-sync.sh [--check-only|--remediate] [--quiet] [--skip-tls-sans] [namespace] [replicas]
-# Env: HOUSING_NS, KAFKA_BROKER_REPLICAS, DRIFT_WRITE_PROM_FILE (forwarded to check-kafka-config-drift)
+# Env: HOUSING_NS=record-platform, KAFKA_BROKER_REPLICAS, DRIFT_WRITE_PROM_FILE (forwarded to check-kafka-config-drift)
 #
 # --check-only (default): exit 0 only if no advertised drift and (unless --skip-tls-sans) TLS SANs match LB IPs.
 # --remediate:          refresh TLS from LB, rollout restart kafka StatefulSet, run verify-kafka-cluster.sh
@@ -41,11 +41,11 @@   esac
 done
 
-NS="${POS_NS:-${HOUSING_NS:-off-campus-housing-tracker}}"
+NS="${POS_NS:-${HOUSING_NS=record-platform:-record-platform}}"
 REP="${POS_REP:-${KAFKA_BROKER_REPLICAS:-3}}"
 
 _run_advert_check() {
-  export HOUSING_NS="$NS"
+  export HOUSING_NS=record-platform="$NS"
   export KAFKA_BROKER_REPLICAS="$REP"
   if [[ "$QUIET" == "1" ]]; then
     bash "$SCRIPT_DIR/check-kafka-config-drift.sh" "$NS" "$REP" >/dev/null 2>&1
@@ -55,7 +55,7 @@ }
 
 _run_tls_check() {
-  export HOUSING_NS="$NS"
+  export HOUSING_NS=record-platform="$NS"
   export KAFKA_BROKER_REPLICAS="$REP"
   if [[ "$QUIET" == "1" ]]; then
     bash "$SCRIPT_DIR/verify-kafka-tls-sans.sh" "$NS" "$REP" >/dev/null 2>&1
@@ -100,7 +100,7 @@ # --- remediate ---
 echo "=== kafka-runtime-sync --remediate (ns=$NS replicas=$REP) ==="
 REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
-HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-refresh-tls-from-lb.sh"
+HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-refresh-tls-from-lb.sh"
 
 echo "▶ Verifying broker JKS before Kafka rollout (PrivateKeyEntry + serverAuth + clientAuth EKU)…"
 chmod +x "$SCRIPT_DIR/verify-kafka-broker-keystore-jks.sh" 2>/dev/null || true
@@ -118,13 +118,13 @@ kubectl rollout status statefulset/kafka -n "$NS" --timeout="$_rollout_to"
 
 chmod +x "$SCRIPT_DIR/kafka-after-rollout-verify-brokers.sh" 2>/dev/null || true
-HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-after-rollout-verify-brokers.sh" || {
+HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-after-rollout-verify-brokers.sh" || {
   echo "❌ Post-rollout broker TLS parity / Ready check failed (see kafka-tls-guard / PKIX logs above)" >&2
   exit 1
 }
 
 echo "▶ Post-rollout drift / SAN check…"
-if ! HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-runtime-sync.sh" --check-only "$NS" "$REP"; then
+if ! HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-runtime-sync.sh" --check-only "$NS" "$REP"; then
   echo "❌ kafka-runtime-sync --check-only failed after remediate rollout" >&2
   exit 1
 fi
@@ -138,7 +138,7 @@   VERIFY_KAFKA_SKIP_QUORUM_GATE=0 \
   VERIFY_KAFKA_SKIP_LEADERSHIP_CHURN_GATE=0 \
   VERIFY_KAFKA_SKIP_BROKER_API_GATE=0 \
-  HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" \
+  HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" \
   bash "$SCRIPT_DIR/verify-kafka-cluster.sh" "$NS" "$REP" || {
   echo "❌ verify-kafka-cluster failed after remediate" >&2
   exit 1

--- a/scripts/kafka-ssl-from-dev-root.sh+++ b/scripts/kafka-ssl-from-dev-root.sh@@ -1,11 +1,11 @@ #!/usr/bin/env bash
 # Create Kafka broker keystore/truststore and kafka-ssl-secret using dev-root-ca (same CA as Caddy).
 # Run after reissue (certs/dev-root.pem and certs/dev-root.key must exist).
-# Output: certs/kafka-ssl/*.jks, *.p12, passwords; creates kafka-ssl-secret in off-campus-housing-tracker
+# Output: certs/kafka-ssl/*.jks, *.p12, passwords; creates kafka-ssl-secret in record-platform
 #   with ca-cert.pem (dev-root), keystore, truststore for Docker Kafka SSL and Node clients.
 #
 # Usage: ./scripts/kafka-ssl-from-dev-root.sh
-#   KAFKA_SSL_NS=off-campus-housing-tracker  — namespace for kafka-ssl-secret
+#   KAFKA_SSL_NS=record-platform  — namespace for kafka-ssl-secret
 #   KAFKA_SSL_PASS=changeit       — keystore/truststore password
 #   KAFKA_BROKER_REPLICAS=3       — SANs for kafka-0..N-1 (headless + external service DNS)
 #   KAFKA_SSL_EXTRA_IP_SANS=      — optional manual IPs; merged with auto-discovered LB IPs when auto is on
@@ -19,7 +19,7 @@ export PATH="${SCRIPT_DIR}/shims:/opt/homebrew/bin:/usr/local/bin:${PATH:-}"
 cd "$REPO_ROOT"
 
-NS="${KAFKA_SSL_NS:-off-campus-housing-tracker}"
+NS="${KAFKA_SSL_NS:-record-platform}"
 PASS="${KAFKA_SSL_PASS:-changeit}"
 CA_PEM="${REPO_ROOT}/certs/dev-root.pem"
 CA_KEY="${REPO_ROOT}/certs/dev-root.key"
@@ -79,7 +79,7 @@ say "1. Generating Kafka broker key and CSR..."
 openssl genrsa -out "$TMP/kafka.key" 2048 2>/dev/null
 openssl req -new -key "$TMP/kafka.key" -out "$TMP/kafka.csr" \
-  -subj "/CN=${CN}/O=off-campus-housing-tracker" 2>/dev/null
+  -subj "/CN=${CN}/O=record-platform" 2>/dev/null
 
 # Broker EKU: serverAuth (listener) + clientAuth (JVM as TLS client for inter-broker SSL, etc.).
 # Omitting clientAuth causes: "Extended key usage does not permit use for TLS client authentication".
@@ -131,7 +131,7 @@ say "3b. Generating Kafka client cert (mTLS: ssl.client.auth=required)..."
 openssl genrsa -out "$TMP/client.key" 2048 2>/dev/null
 openssl req -new -key "$TMP/client.key" -out "$TMP/client.csr" \
-  -subj "/CN=kafka-client/O=off-campus-housing-tracker" 2>/dev/null
+  -subj "/CN=kafka-client/O=record-platform" 2>/dev/null
 cat > "$TMP/client.ext" <<EOF
 [v3_client]
 basicConstraints = CA:FALSE
@@ -190,19 +190,19 @@   fi
 fi
 
-say "4b. Creating och-kafka-ssl-secret (same client material; Deployments mount och-kafka-ssl-secret)…"
-_och_kafka_yaml="${TMP}/och-kafka-ssl-secret.yaml"
-kubectl create secret generic och-kafka-ssl-secret -n "$NS" \
+say "4b. Creating kafka-ssl-secret (same client material; Deployments mount kafka-ssl-secret)…"
+_och_kafka_yaml="${TMP}/kafka-ssl-secret.yaml"
+kubectl create secret generic kafka-ssl-secret -n "$NS" \
   --from-file=ca-cert.pem="$OUT/ca-cert.pem" \
   --from-file=client.crt="$OUT/client.crt" \
   --from-file=client.key="$OUT/client.key" \
   --dry-run=client -o yaml >"$_och_kafka_yaml"
 if kubectl apply -f "$_och_kafka_yaml" --request-timeout=20s 2>/dev/null; then
-  ok "och-kafka-ssl-secret created/updated"
+  ok "kafka-ssl-secret created/updated"
 elif [[ "$ctx" == *"colima"* ]] && command -v colima >/dev/null 2>&1 && [[ -f "$_och_kafka_yaml" ]]; then
-  colima ssh -- env KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f "$_och_kafka_yaml" --request-timeout=20s 2>/dev/null && ok "och-kafka-ssl-secret (via colima ssh)" || warn "och-kafka-ssl-secret apply failed"
+  colima ssh -- env KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f "$_och_kafka_yaml" --request-timeout=20s 2>/dev/null && ok "kafka-ssl-secret (via colima ssh)" || warn "kafka-ssl-secret apply failed"
 else
-  warn "och-kafka-ssl-secret apply failed"
+  warn "kafka-ssl-secret apply failed"
 fi
 
 say "=== Kafka SSL (dev-root-ca) done ==="

--- a/scripts/kafka-sync-metallb.sh+++ b/scripts/kafka-sync-metallb.sh@@ -5,11 +5,11 @@ # If there is no drift (LB ↔ advertised + TLS SAN vs LB), skips TLS regen and rollout and only runs
 # verify-kafka-cluster.sh. Force full remediation: KAFKA_SYNC_METALLB_FORCE=1 or ./scripts/kafka-sync-metallb.sh --force
 #
-# Env: HOUSING_NS, KAFKA_BROKER_REPLICAS, KAFKA_SSL_* (see scripts/kafka-ssl-from-dev-root.sh)
+# Env: HOUSING_NS=record-platform, KAFKA_BROKER_REPLICAS, KAFKA_SSL_* (see scripts/kafka-ssl-from-dev-root.sh)
 set -euo pipefail
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
-NS="${HOUSING_NS:-off-campus-housing-tracker}"
+NS="${HOUSING_NS=record-platform:-record-platform}"
 REP="${KAFKA_BROKER_REPLICAS:-3}"
 FORCE="${KAFKA_SYNC_METALLB_FORCE:-0}"
 
@@ -21,10 +21,10 @@ done
 
 chmod +x "$SCRIPT_DIR/ensure-dev-root-ca.sh" "$SCRIPT_DIR/kafka-runtime-sync.sh" "$SCRIPT_DIR/kafka-refresh-tls-from-lb.sh" "$SCRIPT_DIR/verify-kafka-cluster.sh" "$SCRIPT_DIR/kafka-auto-heal-inter-broker-tls.sh" 2>/dev/null || true
-HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-auto-heal-inter-broker-tls.sh" || exit 1
+HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-auto-heal-inter-broker-tls.sh" || exit 1
 
 if [[ "$FORCE" != "1" ]]; then
-  if HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-runtime-sync.sh" --check-only --quiet "$NS" "$REP"; then
+  if HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-runtime-sync.sh" --check-only --quiet "$NS" "$REP"; then
     echo "✅ kafka-sync-metallb: no runtime drift — skipping TLS regen and rollout (ns=$NS)."
     VERIFY_KAFKA_HEALTH_ONLY=0 \
       VERIFY_KAFKA_SKIP_META_IDENTITY=0 \
@@ -34,7 +34,7 @@       VERIFY_KAFKA_SKIP_QUORUM_GATE=0 \
       VERIFY_KAFKA_SKIP_LEADERSHIP_CHURN_GATE=0 \
       VERIFY_KAFKA_SKIP_BROKER_API_GATE=0 \
-      HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" \
+      HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" \
       bash "$SCRIPT_DIR/verify-kafka-cluster.sh" "$NS" "$REP"
     echo "✅ kafka-sync-metallb complete (verify only)."
     exit 0
@@ -46,7 +46,7 @@ 
 echo "=== kafka-sync-metallb (ns=$NS) ==="
 REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
-HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-refresh-tls-from-lb.sh"
+HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-refresh-tls-from-lb.sh"
 
 echo "▶ Verifying broker JKS before Kafka rollout…"
 chmod +x "$SCRIPT_DIR/verify-kafka-broker-keystore-jks.sh" 2>/dev/null || true
@@ -60,7 +60,7 @@ kubectl rollout status statefulset/kafka -n "$NS" --timeout="${KAFKA_ROLLOUT_TIMEOUT:-600s}"
 
 chmod +x "$SCRIPT_DIR/kafka-after-rollout-verify-brokers.sh" 2>/dev/null || true
-HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-after-rollout-verify-brokers.sh" || exit 1
+HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$SCRIPT_DIR/kafka-after-rollout-verify-brokers.sh" || exit 1
 
 VERIFY_KAFKA_HEALTH_ONLY=0 \
   VERIFY_KAFKA_SKIP_META_IDENTITY=0 \
@@ -70,7 +70,7 @@   VERIFY_KAFKA_SKIP_QUORUM_GATE=0 \
   VERIFY_KAFKA_SKIP_LEADERSHIP_CHURN_GATE=0 \
   VERIFY_KAFKA_SKIP_BROKER_API_GATE=0 \
-  HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" \
+  HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" \
   bash "$SCRIPT_DIR/verify-kafka-cluster.sh" "$NS" "$REP"
 
 echo "✅ kafka-sync-metallb complete"

--- a/scripts/kafka-tls-guard.sh+++ b/scripts/kafka-tls-guard.sh@@ -5,7 +5,7 @@ # Broker SAN contract (DNS + MetalLB IPs): scripts/lib/kafka-broker-sans.sh — consumed by that script and verify-kafka-tls-sans.sh.
 #
 # Env:
-#   HOUSING_NS — default off-campus-housing-tracker
+#   HOUSING_NS=record-platform — default record-platform
 #   KAFKA_BROKER_REPLICAS — default 3
 #   KAFKA_TLS_GUARD_SKIP_VERIFY — set 1 to skip make verify-kafka-cluster at end
 #   KAFKA_TLS_GUARD_SKIP_LOG_SCAN — set 1 to skip recent SSL handshake failure grep
@@ -15,7 +15,7 @@ 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
-NS="${HOUSING_NS:-off-campus-housing-tracker}"
+NS="${HOUSING_NS=record-platform:-record-platform}"
 R="${KAFKA_BROKER_REPLICAS:-3}"
 
 command -v kubectl >/dev/null 2>&1 || { echo "❌ kubectl required"; exit 1; }
@@ -213,17 +213,17 @@ kubectl get secret kafka-ssl-secret -n "$NS" -o jsonpath='{.data.ca-cert\.pem}' --request-timeout=25s | base64 -d >"$TMP/broker-ca.pem"
 BROKER_FP="$(_ca_fp_from_pem "$TMP/broker-ca.pem")"
 
-say "6) och-kafka-ssl-secret CA vs kafka-ssl-secret"
-if kubectl get secret och-kafka-ssl-secret -n "$NS" --request-timeout=15s >/dev/null 2>&1; then
-  kubectl get secret och-kafka-ssl-secret -n "$NS" -o jsonpath='{.data.ca-cert\.pem}' --request-timeout=25s | base64 -d >"$TMP/service-ca.pem"
+say "6) kafka-ssl-secret CA vs kafka-ssl-secret"
+if kubectl get secret kafka-ssl-secret -n "$NS" --request-timeout=15s >/dev/null 2>&1; then
+  kubectl get secret kafka-ssl-secret -n "$NS" -o jsonpath='{.data.ca-cert\.pem}' --request-timeout=25s | base64 -d >"$TMP/service-ca.pem"
   SERVICE_FP="$(_ca_fp_from_pem "$TMP/service-ca.pem")"
   if [[ -z "$BROKER_FP" || -z "$SERVICE_FP" || "$BROKER_FP" != "$SERVICE_FP" ]]; then
-    bad "CA fingerprint mismatch kafka-ssl-secret vs och-kafka-ssl-secret (run: make kafka-refresh-tls-from-lb)"
+    bad "CA fingerprint mismatch kafka-ssl-secret vs kafka-ssl-secret (run: make kafka-refresh-tls-from-lb)"
     exit 1
   fi
   ok "Service client secret CA matches broker secret"
 else
-  echo "   ℹ️  och-kafka-ssl-secret absent — skipped"
+  echo "   ℹ️  kafka-ssl-secret absent — skipped"
 fi
 
 _ann="$(kubectl get secret kafka-ssl-secret -n "$NS" -o go-template='{{index .metadata.annotations "och.dev/ca-fingerprint-sha256"}}' 2>/dev/null | tr -d '\r' || true)"

--- a/scripts/kafka-tls-rotate-atomic.sh+++ b/scripts/kafka-tls-rotate-atomic.sh@@ -2,12 +2,12 @@ # Atomic Kafka TLS refresh: no broker runs while kafka-ssl-secret is replaced (avoids JKS/truststore drift across replicas).
 # Uses existing dev-root CA + kafka-refresh-tls-from-lb (MetalLB SANs). Does NOT create a new CA.
 #
-# Env: HOUSING_NS, KAFKA_BROKER_REPLICAS (default 3), KAFKA_TLS_ROTATE_SKIP_GUARD=1 to skip kafka-tls-guard at end
+# Env: HOUSING_NS=record-platform, KAFKA_BROKER_REPLICAS (default 3), KAFKA_TLS_ROTATE_SKIP_GUARD=1 to skip kafka-tls-guard at end
 set -euo pipefail
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
-NS="${HOUSING_NS:-off-campus-housing-tracker}"
+NS="${HOUSING_NS=record-platform:-record-platform}"
 R="${KAFKA_BROKER_REPLICAS:-3}"
 
 cd "$REPO_ROOT"
@@ -27,7 +27,7 @@   kubectl wait --for=delete "pod/kafka-$i" -n "$NS" --timeout=300s 2>/dev/null || true
 done
 
-echo "▶ Regenerate kafka-ssl-secret + och-kafka-ssl-secret (full JKS + PEM)"
+echo "▶ Regenerate kafka-ssl-secret + kafka-ssl-secret (full JKS + PEM)"
 bash "$SCRIPT_DIR/kafka-refresh-tls-from-lb.sh"
 
 echo "▶ Scale Kafka back to $R"

--- a/scripts/lib/kafka-kraft-quorum-ok.sh+++ b/scripts/lib/kafka-kraft-quorum-ok.sh@@ -5,7 +5,7 @@ # Intentionally no set -euo: this file is sourced by other scripts.
 
 och_kafka_kraft_quorum_ok() {
-  local NS="${1:-${HOUSING_NS:-off-campus-housing-tracker}}"
+  local NS="${1:-${HOUSING_NS=record-platform:-record-platform}}"
   local EXEC_TO="${KAFKA_CLUSTER_EXEC_TIMEOUT:-45}"
   local _qout
   if ! kubectl get pod kafka-0 -n "$NS" --request-timeout=20s >/dev/null 2>&1; then

--- a/scripts/preflight-kafka-k8s-rollout.sh+++ b/scripts/preflight-kafka-k8s-rollout.sh@@ -6,7 +6,7 @@ # Env: KAFKA_K8S_NS, KAFKA_K8S_POD, KAFKA_K8S_SKIP_API_HEALTH=1 — same as create-kafka-event-topics-k8s.sh
 set -euo pipefail
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
-NS="${KAFKA_K8S_NS:-off-campus-housing-tracker}"
+NS="${KAFKA_K8S_NS:-record-platform}"
 KPOD="${KAFKA_K8S_POD:-kafka-0}"
 BS="kafka-0.kafka:9093"
 

--- a/scripts/tests/kafka-alignment-suite.sh+++ b/scripts/tests/kafka-alignment-suite.sh@@ -7,7 +7,7 @@ # Usage:
 #   ./scripts/tests/kafka-alignment-suite.sh
 # Env:
-#   HOUSING_NS, KAFKA_BROKER_REPLICAS
+#   HOUSING_NS=record-platform, KAFKA_BROKER_REPLICAS
 #   KAFKA_ALIGNMENT_TEST_MODE=1 — run destructive/mutating tests (2–4, 6–7). Otherwise only safe tests (1 + 5).
 #   After each mutating test (3–4, 6–7): kafka-runtime-sync --check-only + verify-kafka-cluster (fail fast).
 #   KAFKA_ALIGNMENT_SKIP_TEST1_VERIFY=1 — test1 runs kafka-runtime-sync --check-only only (skip verify-kafka-cluster).
@@ -27,7 +27,7 @@ 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"
-NS="${HOUSING_NS:-off-campus-housing-tracker}"
+NS="${HOUSING_NS=record-platform:-record-platform}"
 REP="${KAFKA_BROKER_REPLICAS:-3}"
 STAMP="$(date +%Y%m%d-%H%M%S)"
 LOG="${KAFKA_ALIGNMENT_LOG:-$REPO_ROOT/bench_logs/kafka-alignment-suite-$STAMP.log}"
@@ -116,7 +116,7 @@   say "Post-destructive verify ($tag): kafka-runtime-sync --check-only + verify-kafka-cluster"
 
   _post_verify_once() {
-    HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --check-only "$NS" "$REP" \
+    HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --check-only "$NS" "$REP" \
       && VERIFY_KAFKA_HEALTH_ONLY=0 \
         VERIFY_KAFKA_SKIP_META_IDENTITY=0 \
         VERIFY_KAFKA_SKIP_TLS_SANS=0 \
@@ -125,7 +125,7 @@         VERIFY_KAFKA_SKIP_QUORUM_GATE=0 \
         VERIFY_KAFKA_SKIP_LEADERSHIP_CHURN_GATE=0 \
         VERIFY_KAFKA_SKIP_BROKER_API_GATE=0 \
-        HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" \
+        HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" \
         bash "$REPO_ROOT/scripts/verify-kafka-cluster.sh" "$NS" "$REP"
   }
 
@@ -140,7 +140,7 @@   fi
 
   warn "Post-destructive ($tag): running kafka-runtime-sync --remediate, then re-verify…"
-  if ! HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --remediate "$NS" "$REP"; then
+  if ! HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --remediate "$NS" "$REP"; then
     bad "Post-destructive: kafka-runtime-sync --remediate failed ($tag)"
     return 1
   fi
@@ -157,7 +157,7 @@ assert_cluster_green_no_remediate() {
   local tag="$1"
   say "Cluster green verify ($tag): kafka-runtime-sync --check-only + verify-kafka-cluster"
-  HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --check-only "$NS" "$REP" \
+  HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --check-only "$NS" "$REP" \
     && VERIFY_KAFKA_HEALTH_ONLY=0 \
       VERIFY_KAFKA_SKIP_META_IDENTITY=0 \
       VERIFY_KAFKA_SKIP_TLS_SANS=0 \
@@ -166,7 +166,7 @@       VERIFY_KAFKA_SKIP_QUORUM_GATE=0 \
       VERIFY_KAFKA_SKIP_LEADERSHIP_CHURN_GATE=0 \
       VERIFY_KAFKA_SKIP_BROKER_API_GATE=0 \
-      HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" \
+      HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" \
       bash "$REPO_ROOT/scripts/verify-kafka-cluster.sh" "$NS" "$REP"
 }
 
@@ -211,7 +211,7 @@   [[ "${KAFKA_ALIGNMENT_PREFLIGHT_REMEDIATE:-1}" != "1" ]] && return 0
   say "Pre-suite — kafka-runtime-sync --check-only (advertised.listeners vs LB + TLS SAN vs MetalLB)"
   set +e
-  HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --check-only --quiet "$NS" "$REP"
+  HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --check-only --quiet "$NS" "$REP"
   local _pre_rc=$?
   set -e
   if [[ "$_pre_rc" -eq 0 ]]; then
@@ -219,7 +219,7 @@     return 0
   fi
   warn "Pre-suite: drift or SAN/LB TLS mismatch — running kafka-runtime-sync --remediate (refresh from LB IPs, rollout, verify)…"
-  if ! HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --remediate "$NS" "$REP"; then
+  if ! HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --remediate "$NS" "$REP"; then
     bad "Pre-suite: kafka-runtime-sync --remediate failed"
     exit 1
   fi
@@ -235,7 +235,7 @@   [[ "${KAFKA_ALIGNMENT_SKIP_TLS_PARITY_GUARD:-0}" == "1" ]] && return 0
   say "Pre-suite — cross-broker TLS parity (truststore/keystore + PKIX log tail)"
   chmod +x "$REPO_ROOT/scripts/kafka-after-rollout-verify-brokers.sh" 2>/dev/null || true
-  HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-after-rollout-verify-brokers.sh" || {
+  HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-after-rollout-verify-brokers.sh" || {
     bad "Cross-broker TLS parity failed — mixed trust anchor after partial restart? Try: kubectl delete pod -n $NS kafka-0 kafka-1 kafka-2"
     exit 1
   }
@@ -244,7 +244,7 @@ # --- TEST 1: baseline ---
 _test1_run_baseline_checks() {
   if [[ "${KAFKA_ALIGNMENT_SKIP_TEST1_VERIFY:-0}" == "1" ]]; then
-    HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --check-only "$NS" "$REP"
+    HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --check-only "$NS" "$REP"
     return $?
   fi
   VERIFY_KAFKA_HEALTH_ONLY=0 \
@@ -255,9 +255,9 @@       VERIFY_KAFKA_SKIP_QUORUM_GATE=0 \
       VERIFY_KAFKA_SKIP_LEADERSHIP_CHURN_GATE=0 \
       VERIFY_KAFKA_SKIP_BROKER_API_GATE=0 \
-      HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" \
+      HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" \
       bash "$REPO_ROOT/scripts/verify-kafka-cluster.sh" "$NS" "$REP" \
-    && HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --check-only "$NS" "$REP"
+    && HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --check-only "$NS" "$REP"
 }
 
 run_test1_baseline() {
@@ -271,7 +271,7 @@   fi
   if [[ "${KAFKA_ALIGNMENT_BASELINE_REMEDIATE_RETRY:-1}" == "1" ]]; then
     warn "TEST 1 baseline failed — kafka-runtime-sync --remediate, then single retry…"
-    if ! HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --remediate "$NS" "$REP"; then
+    if ! HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --remediate "$NS" "$REP"; then
       emit_result "$name" "FAIL" "$SECONDS"
       return 1
     fi
@@ -312,7 +312,7 @@       return 1
     }
   set +e
-  HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --check-only "$NS" "$REP"
+  HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --check-only "$NS" "$REP"
   rc=$?
   set -e
   if [[ "$rc" -eq 0 ]]; then
@@ -335,7 +335,7 @@   local name="auto_remediate"
   SECONDS=0
   say "TEST 3 — Auto-remediation (kafka-runtime-sync --remediate)"
-  if ! HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --remediate "$NS" "$REP"; then
+  if ! HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --remediate "$NS" "$REP"; then
     kubectl exec -n "$NS" kafka-0 -c "$KAFKA_C" --request-timeout=45s -- sh -c \
       'test -f /etc/kafka/kafka.properties.suite.bak && cp -f /etc/kafka/kafka.properties.suite.bak /etc/kafka/kafka.properties' 2>/dev/null || true
     emit_result "$name" "FAIL" "$SECONDS"
@@ -374,13 +374,13 @@     w=$((w + 2))
   done
   set +e
-  HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --check-only "$NS" "$REP"
+  HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --check-only "$NS" "$REP"
   _drift_rc=$?
   set -e
   if [[ "$_drift_rc" -eq 0 ]]; then
     warn "Check-only still passed after LB churn — continuing to remediation anyway"
   fi
-  if ! HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --remediate "$NS" "$REP"; then
+  if ! HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --remediate "$NS" "$REP"; then
     emit_result "$name" "FAIL" "$SECONDS"
     return 1
   fi
@@ -419,7 +419,7 @@     return 1
   }
   set +e
-  KAFKA_TLS_PEM="$bad_pem" HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" \
+  KAFKA_TLS_PEM="$bad_pem" HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" \
     bash "$REPO_ROOT/scripts/verify-kafka-tls-sans.sh" "$NS" "$REP"
   tls_rc=$?
   set -e
@@ -497,7 +497,7 @@ 
 if [[ "$TESTS_FAILED" -gt 0 ]] && [[ "${KAFKA_ALIGNMENT_SUITE_FAIL_REMEDIATE:-1}" == "1" ]]; then
   say "Suite had failing test(s); running kafka-runtime-sync --remediate (TLS from LB + rollout + verify)…"
-  if ! HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --remediate "$NS" "$REP"; then
+  if ! HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REP" bash "$REPO_ROOT/scripts/kafka-runtime-sync.sh" --remediate "$NS" "$REP"; then
     bad "Suite-level kafka-runtime-sync --remediate failed"
     print_summary
     write_prom_file || true

--- a/scripts/validate-kafka-dns.sh+++ b/scripts/validate-kafka-dns.sh@@ -3,7 +3,7 @@ # Stale slices → wrong DNS A records → KRaft :9095 "could not be established".
 set -euo pipefail
 
-NAMESPACE="${KAFKA_NAMESPACE:-off-campus-housing-tracker}"
+NAMESPACE="${KAFKA_NAMESPACE:-record-platform}"
 SERVICE_LABEL="kubernetes.io/service-name=kafka"
 
 echo "Checking Kafka headless DNS vs EndpointSlices (namespace=${NAMESPACE})..."

--- a/scripts/validate-kafka-stack-contract.sh+++ b/scripts/validate-kafka-stack-contract.sh@@ -11,7 +11,7 @@ #   KAFKA_CONTRACT_MIN_CHAOS_SCORE=0.85 — fail if kafka-contract chaosReadinessScore is below threshold (needs jq)
 #   KAFKA_CONTRACT_LIVE_TARGET=k8s — skip docker compose live path; validate via kubectl + kafka-contract (MetalLB / in-cluster)
 #     Requires: kubectl, KAFKA_BROKER, KAFKA_SSL_ENABLED=true, KAFKA_CA_CERT, KAFKA_CLIENT_CERT, KAFKA_CLIENT_KEY
-#   KAFKA_CONTRACT_K8S_NS — default off-campus-housing-tracker
+#   KAFKA_CONTRACT_K8S_NS — default record-platform
 #   KAFKA_CONTRACT_K8S_WAIT_PODS=0 — skip kubectl wait for kafka-0..2 Ready (e.g. chaos drills)
 #   PREFLIGHT_SKIP_* — passed through to verify-jks / wait script where applicable
 set -euo pipefail
@@ -119,7 +119,7 @@   say "Live target: k8s (skipping docker compose Kafka)"
   command -v kubectl >/dev/null 2>&1 || fail "kubectl required for KAFKA_CONTRACT_LIVE_TARGET=k8s"
   command -v jq >/dev/null 2>&1 || fail "jq required for KAFKA_CONTRACT_LIVE_TARGET=k8s"
-  _kns="${KAFKA_CONTRACT_K8S_NS:-off-campus-housing-tracker}"
+  _kns="${KAFKA_CONTRACT_K8S_NS:-record-platform}"
   [[ -n "${KAFKA_BROKER:-}" ]] || fail "KAFKA_CONTRACT_LIVE_TARGET=k8s requires KAFKA_BROKER (e.g. MetalLB-IP:9094)"
   [[ "${KAFKA_SSL_ENABLED:-}" == "true" ]] || fail "KAFKA_CONTRACT_LIVE_TARGET=k8s requires KAFKA_SSL_ENABLED=true"
   for _v in KAFKA_CA_CERT KAFKA_CLIENT_CERT KAFKA_CLIENT_KEY; do

--- a/scripts/verify-cluster-kafka-three-brokers.sh+++ b/scripts/verify-cluster-kafka-three-brokers.sh@@ -1,19 +1,19 @@ #!/usr/bin/env bash
 # Fail if ConfigMap app-config KAFKA_BROKER does not list kafka-0..2 headless DNS seeds (three brokers).
 # Usage: ./scripts/verify-cluster-kafka-three-brokers.sh
-#   HOUSING_NS=off-campus-housing-tracker (default)
+#   HOUSING_NS=record-platform=record-platform (default)
 set -euo pipefail
 
-HOUSING_NS="${HOUSING_NS:-off-campus-housing-tracker}"
+HOUSING_NS=record-platform="${HOUSING_NS=record-platform:-record-platform}"
 
 if ! command -v kubectl >/dev/null 2>&1; then
   echo "kubectl not found" >&2
   exit 1
 fi
 
-raw="$(kubectl -n "$HOUSING_NS" get configmap app-config -o jsonpath='{.data.KAFKA_BROKER}' 2>/dev/null || true)"
+raw="$(kubectl -n "$HOUSING_NS=record-platform" get configmap app-config -o jsonpath='{.data.KAFKA_BROKER}' 2>/dev/null || true)"
 if [[ -z "${raw// /}" ]]; then
-  echo "verify-kafka-three-brokers: missing ConfigMap app-config.data.KAFKA_BROKER in $HOUSING_NS" >&2
+  echo "verify-kafka-three-brokers: missing ConfigMap app-config.data.KAFKA_BROKER in $HOUSING_NS=record-platform" >&2
   exit 1
 fi
 

--- a/scripts/verify-kafka-cluster.sh+++ b/scripts/verify-kafka-cluster.sh@@ -13,9 +13,9 @@ # Optional: VERIFY_KAFKA_INCLUDE_GATEWAY_NC=1 — also nc -vz kafka 9093 from first api-gateway pod (stricter path check).
 #
 # Phase 6a2c6 — CA / broker trust (fail fast on secret drift):
-#   Compares SHA-256 fingerprints of ca-cert.pem in kafka-ssl-secret vs och-kafka-ssl-secret.
+#   Compares SHA-256 fingerprints of ca-cert.pem in kafka-ssl-secret vs kafka-ssl-secret.
 #   Verifies kafka-broker.pem chains to that CA (openssl verify).
-#   VERIFY_KAFKA_CHECK_CLIENT_DEPLOY_MOUNTS=1 — require listed Deployments to reference och-kafka-ssl-secret (after deploy-dev).
+#   VERIFY_KAFKA_CHECK_CLIENT_DEPLOY_MOUNTS=1 — require listed Deployments to reference kafka-ssl-secret (after deploy-dev).
 #   VERIFY_KAFKA_CLIENT_MOUNTS_ONLY=1 — run only the deployment mount check and exit (dev-onboard Phase 6b).
 #   VERIFY_KAFKA_MOUNT_REQUIRED_DEPLOYS — space-separated names (default: Kafka consumer services in base/).
 #
@@ -37,11 +37,11 @@ #
 # Usage:
 #   ./scripts/verify-kafka-cluster.sh [namespace] [replicas]
-# Env: HOUSING_NS, KAFKA_BROKER_REPLICAS, KAFKA_CHURN_LOG_SINCE (default 10m for renounce scan)
+# Env: HOUSING_NS=record-platform, KAFKA_BROKER_REPLICAS, KAFKA_CHURN_LOG_SINCE (default 10m for renounce scan)
 set -euo pipefail
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
-NS="${1:-${HOUSING_NS:-off-campus-housing-tracker}}"
+NS="${1:-${HOUSING_NS=record-platform:-record-platform}}"
 REPLICAS="${2:-${KAFKA_BROKER_REPLICAS:-3}}"
 CHURN_SINCE="${KAFKA_CHURN_LOG_SINCE:-10m}"
 EXEC_TO="${KAFKA_CLUSTER_EXEC_TIMEOUT:-45}"
@@ -74,11 +74,11 @@       _missing=1
       continue
     fi
-    if ! kubectl get deploy "$_d" -n "$NS" -o yaml --request-timeout=25s | grep -q "secretName: och-kafka-ssl-secret"; then
-      bad "Deployment $_d does not mount secret och-kafka-ssl-secret"
+    if ! kubectl get deploy "$_d" -n "$NS" -o yaml --request-timeout=25s | grep -q "secretName: kafka-ssl-secret"; then
+      bad "Deployment $_d does not mount secret kafka-ssl-secret"
       _missing=1
     else
-      ok "Deployment $_d references och-kafka-ssl-secret"
+      ok "Deployment $_d references kafka-ssl-secret"
     fi
   done < <(tr ' ' '\n' <<<"$(_mount_required_deploys)" | grep -v '^$')
   [[ "$_missing" -eq 0 ]] || exit 1
@@ -143,7 +143,7 @@ if [[ "${VERIFY_KAFKA_SKIP_TLS_SANS:-0}" != "1" ]]; then
   say "Phase 6a2c — TLS SAN verification"
   chmod +x "$SCRIPT_DIR/verify-kafka-tls-sans.sh" 2>/dev/null || true
-  HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REPLICAS" bash "$SCRIPT_DIR/verify-kafka-tls-sans.sh" "$NS" "$REPLICAS"
+  HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REPLICAS" bash "$SCRIPT_DIR/verify-kafka-tls-sans.sh" "$NS" "$REPLICAS"
   ok "TLS SAN verification passed"
 else
   say "Phase 6a2c — skipped (VERIFY_KAFKA_SKIP_TLS_SANS=1 or VERIFY_KAFKA_HEALTH_ONLY=1)"
@@ -152,30 +152,30 @@ if [[ "${VERIFY_KAFKA_SKIP_ADVERTISED:-0}" != "1" ]]; then
   say "Phase 6a2c2 — advertised.listeners verification"
   chmod +x "$SCRIPT_DIR/verify-kafka-kraft-advertised-listeners.sh" 2>/dev/null || true
-  HOUSING_NS="$NS" KAFKA_BROKER_REPLICAS="$REPLICAS" bash "$SCRIPT_DIR/verify-kafka-kraft-advertised-listeners.sh" "$NS" "$REPLICAS"
+  HOUSING_NS=record-platform="$NS" KAFKA_BROKER_REPLICAS="$REPLICAS" bash "$SCRIPT_DIR/verify-kafka-kraft-advertised-listeners.sh" "$NS" "$REPLICAS"
   ok "advertised.listeners verification passed"
 else
   say "Phase 6a2c2 — skipped (VERIFY_KAFKA_SKIP_ADVERTISED=1 or VERIFY_KAFKA_HEALTH_ONLY=1)"
 fi
 
 if [[ "${VERIFY_KAFKA_SKIP_TLS_CONSISTENCY:-0}" != "1" ]]; then
-  say "Phase 6a2c6 — CA fingerprint consistency (kafka-ssl-secret vs och-kafka-ssl-secret) + broker chain"
+  say "Phase 6a2c6 — CA fingerprint consistency (kafka-ssl-secret vs kafka-ssl-secret) + broker chain"
   _tmp="$(mktemp -d)"
   # shellcheck disable=SC2064
   trap 'rm -rf "$_tmp"' EXIT
   kubectl get secret kafka-ssl-secret -n "$NS" -o jsonpath='{.data.ca-cert\.pem}' --request-timeout=25s | base64 -d >"$_tmp/broker-ca.pem"
-  kubectl get secret och-kafka-ssl-secret -n "$NS" -o jsonpath='{.data.ca-cert\.pem}' --request-timeout=25s | base64 -d >"$_tmp/service-ca.pem"
+  kubectl get secret kafka-ssl-secret -n "$NS" -o jsonpath='{.data.ca-cert\.pem}' --request-timeout=25s | base64 -d >"$_tmp/service-ca.pem"
   kubectl get secret kafka-ssl-secret -n "$NS" -o jsonpath='{.data.kafka-broker\.pem}' --request-timeout=25s | base64 -d >"$_tmp/kafka-broker.pem"
   if [[ ! -s "$_tmp/broker-ca.pem" || ! -s "$_tmp/service-ca.pem" || ! -s "$_tmp/kafka-broker.pem" ]]; then
-    bad "Missing ca-cert.pem or kafka-broker.pem in secrets (kafka-ssl-secret / och-kafka-ssl-secret)"
+    bad "Missing ca-cert.pem or kafka-broker.pem in secrets (kafka-ssl-secret / kafka-ssl-secret)"
     exit 1
   fi
   _bfp="$(openssl x509 -in "$_tmp/broker-ca.pem" -noout -fingerprint -sha256 2>/dev/null | cut -d= -f2)"
   _sfp="$(openssl x509 -in "$_tmp/service-ca.pem" -noout -fingerprint -sha256 2>/dev/null | cut -d= -f2)"
   if [[ -z "$_bfp" || -z "$_sfp" || "$_bfp" != "$_sfp" ]]; then
-    bad "CA fingerprint mismatch — broker secret vs och-kafka-ssl-secret (Node clients will reject broker TLS)"
+    bad "CA fingerprint mismatch — broker secret vs kafka-ssl-secret (Node clients will reject broker TLS)"
     echo "   kafka-ssl-secret ca:     ${_bfp:-?}" >&2
-    echo "   och-kafka-ssl-secret ca: ${_sfp:-?}" >&2
+    echo "   kafka-ssl-secret ca: ${_sfp:-?}" >&2
     exit 1
   fi
   ok "CA fingerprints match ($_bfp)"
@@ -209,7 +209,7 @@ fi
 
 if [[ "${VERIFY_KAFKA_CHECK_CLIENT_DEPLOY_MOUNTS:-0}" == "1" ]]; then
-  say "Phase 6a2c6b — Deployments must reference och-kafka-ssl-secret"
+  say "Phase 6a2c6b — Deployments must reference kafka-ssl-secret"
   _missing=0
   while read -r _d; do
     [[ -z "$_d" ]] && continue
@@ -218,11 +218,11 @@       _missing=1
       continue
     fi
-    if ! kubectl get deploy "$_d" -n "$NS" -o yaml --request-timeout=25s | grep -q "secretName: och-kafka-ssl-secret"; then
-      bad "Deployment $_d does not mount secret och-kafka-ssl-secret"
+    if ! kubectl get deploy "$_d" -n "$NS" -o yaml --request-timeout=25s | grep -q "secretName: kafka-ssl-secret"; then
+      bad "Deployment $_d does not mount secret kafka-ssl-secret"
       _missing=1
     else
-      ok "Deployment $_d references och-kafka-ssl-secret"
+      ok "Deployment $_d references kafka-ssl-secret"
     fi
   done < <(tr ' ' '\n' <<<"$(_mount_required_deploys)" | grep -v '^$')
   [[ "$_missing" -eq 0 ]] || exit 1

--- a/scripts/verify-kafka-event-topic-partitions.sh+++ b/scripts/verify-kafka-event-topic-partitions.sh@@ -33,7 +33,7 @@ 
 if [[ "${KAFKA_PARTITION_VERIFY_TARGET:-}" == "k8s" ]]; then
   command -v kubectl >/dev/null 2>&1 || fail "kubectl required for KAFKA_PARTITION_VERIFY_TARGET=k8s"
-  _ns="${KAFKA_K8S_NS:-off-campus-housing-tracker}"
+  _ns="${KAFKA_K8S_NS:-record-platform}"
   _pod="${KAFKA_K8S_POD:-kafka-0}"
   kubectl get pod "$_pod" -n "$_ns" --request-timeout=20s >/dev/null 2>&1 || fail "Pod $_pod not in $_ns"
   _inner_props='TS_PASS=$(cat /etc/kafka/secrets/kafka.truststore-password)

--- a/scripts/verify-kafka-kraft-advertised-listeners.sh+++ b/scripts/verify-kafka-kraft-advertised-listeners.sh@@ -7,12 +7,12 @@ # Usage:
 #   ./scripts/verify-kafka-kraft-advertised-listeners.sh [namespace] [replicas]
 # Env:
-#   HOUSING_NS / namespace arg
+#   HOUSING_NS=record-platform / namespace arg
 #   KAFKA_BROKER_REPLICAS / replicas arg
 #   KAFKA_ADVERTISED_VERIFY_TIMEOUT — kubectl exec timeout seconds (default 25)
 set -euo pipefail
 
-NS="${1:-${HOUSING_NS:-off-campus-housing-tracker}}"
+NS="${1:-${HOUSING_NS=record-platform:-record-platform}}"
 REPLICAS="${2:-${KAFKA_BROKER_REPLICAS:-3}}"
 EXEC_TO="${KAFKA_ADVERTISED_VERIFY_TIMEOUT:-25}"
 

--- a/scripts/verify-kafka-kraft-e2e.sh+++ b/scripts/verify-kafka-kraft-e2e.sh@@ -4,7 +4,7 @@ # Safe by default: read-only checks + optional TLS + kafka-contract. Destructive phases require explicit env.
 #
 # Env (common):
-#   KRAFT_E2E_NS=off-campus-housing-tracker
+#   KRAFT_E2E_NS=record-platform
 #   Use kubectl on PATH or KUBECONFIG pointing at your k3s cluster (Colima: kubectl context colima).
 #
 # Phase 0 (destructive): KRAFT_E2E_PHASE0_SCALE_LEGACY_KAFKA=1 — kubectl scale deploy/kafka --replicas=0
@@ -27,7 +27,7 @@ REPO_ROOT="${REPO_ROOT:-$(cd "$SCRIPT_DIR/.." && pwd)}"
 cd "$REPO_ROOT"
 
-NS="${KRAFT_E2E_NS:-off-campus-housing-tracker}"
+NS="${KRAFT_E2E_NS:-record-platform}"
 say() { printf "\n\033[1m%s\033[0m\n" "$*"; }
 pass() { echo "[OK] $*"; }
 fail() { echo "[FAIL] $*" >&2; exit 1; }

--- a/scripts/verify-kafka-no-static-advertised-env.sh+++ b/scripts/verify-kafka-no-static-advertised-env.sh@@ -11,7 +11,7 @@ # Usage: ./scripts/verify-kafka-no-static-advertised-env.sh [namespace]
 set -euo pipefail
 
-NS="${1:-${HOUSING_NS:-off-campus-housing-tracker}}"
+NS="${1:-${HOUSING_NS=record-platform:-record-platform}}"
 
 say() { printf "\n\033[1m%s\033[0m\n" "$*"; }
 ok() { echo "✅ $*"; }

--- a/scripts/verify-kafka-tls-sans.sh+++ b/scripts/verify-kafka-tls-sans.sh@@ -13,7 +13,7 @@ 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
-NS="${1:-${HOUSING_NS:-off-campus-housing-tracker}}"
+NS="${1:-${HOUSING_NS=record-platform:-record-platform}}"
 REPLICAS="${2:-${KAFKA_BROKER_REPLICAS:-3}}"
 
 # shellcheck source=lib/kafka-broker-sans.sh

--- a/scripts/verify-preflight-edge-routing.sh+++ b/scripts/verify-preflight-edge-routing.sh@@ -5,7 +5,7 @@ # Usage:
 #   ./scripts/verify-preflight-edge-routing.sh [namespace] [edge_hostname]
 # Env:
-#   HOUSING_NS, OCH_EDGE_HOSTNAME (default off-campus-housing.test)
+#   HOUSING_NS=record-platform, OCH_EDGE_HOSTNAME (default record.test)
 #   VERIFY_PREFLIGHT_EDGE_PHASES — comma list: ingress,dns,curl (default: all three)
 #   PREFLIGHT_SKIP_EDGE_INGRESS_PARITY_GATE=1 — skip ingress
 #   PREFLIGHT_SKIP_EDGE_DNS_LB_GATE=1 — skip dns
@@ -22,9 +22,9 @@ # shellcheck disable=SC1091
 source "$SCRIPT_DIR/lib/edge-test-url.sh"
 
-NS="${1:-${HOUSING_NS:-off-campus-housing-tracker}}"
-HOST="${2:-${OCH_EDGE_HOSTNAME:-off-campus-housing.test}}"
-ING_NAME="${EDGE_INGRESS_NAME:-off-campus-housing-tracker}"
+NS="${1:-${HOUSING_NS=record-platform:-record-platform}}"
+HOST="${2:-${OCH_EDGE_HOSTNAME:-record.test}}"
+ING_NAME="${EDGE_INGRESS_NAME:-record-platform}"
 ING_NS="${EDGE_INGRESS_NAMESPACE:-$NS}"
 
 say() { printf "\n\033[1m%s\033[0m\n" "$*"; }
@@ -66,7 +66,7 @@   python3 <<'PY'
 import json, os, sys
 
-want_host = os.environ.get("_EDGE_VERIFY_HOST", "off-campus-housing.test")
+want_host = os.environ.get("_EDGE_VERIFY_HOST", "record.test")
 path = os.environ["_EDGE_ING_JSON_FILE"]
 with open(path, encoding="utf-8") as f:
     doc = json.load(f)
@@ -149,7 +149,7 @@ if [[ "$RUN_INGRESS" -eq 1 ]]; then
   say "Edge 6b1 — Ingress path parity ($ING_NAME/$ING_NS, host $HOST)"
   _verify_ingress_python || exit 1
-  ok "Ingress: /api + /auth → api-gateway:4020, / → nginx:8080 (ordered before catch-all /)"
+  ok "Ingress: /api + /auth → api-gateway:4000, / → nginx:8080 (ordered before catch-all /)"
 fi
 
 if [[ "$RUN_DNS" -eq 1 ]]; then

--- a/scripts/wait-for-kafka-external-lb-ips.sh+++ b/scripts/wait-for-kafka-external-lb-ips.sh@@ -1,9 +1,9 @@ #!/usr/bin/env bash
 # Wait until kafka-0..N-1-external each have status.loadBalancer.ingress[0].ip (MetalLB / cloud LB).
-# Env: HOUSING_NS, KAFKA_BROKER_REPLICAS, KAFKA_LB_WAIT_MAX_ATTEMPTS (default 90), KAFKA_LB_WAIT_SLEEP (default 2)
+# Env: HOUSING_NS=record-platform, KAFKA_BROKER_REPLICAS, KAFKA_LB_WAIT_MAX_ATTEMPTS (default 90), KAFKA_LB_WAIT_SLEEP (default 2)
 set -euo pipefail
 
-NS="${HOUSING_NS:-off-campus-housing-tracker}"
+NS="${HOUSING_NS=record-platform:-record-platform}"
 REP="${KAFKA_BROKER_REPLICAS:-3}"
 MAX="${KAFKA_LB_WAIT_MAX_ATTEMPTS:-90}"
 SLEEP="${KAFKA_LB_WAIT_SLEEP:-2}"
