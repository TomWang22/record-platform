# OCH → RP namespace patch (generated; NOT applied)
# Matrix: /Users/tom/record-platform/docs/bundles/OCH_TO_RP_CONVERSION_MATRIX.md
# Staging: /Users/tom/bundle-staging/record-platform-och-vitest-playwright-event-layer-analytics-reference-20260409
# Review with: git apply --check <this-file>

--- a/package.json+++ b/package.json@@ -1,5 +1,5 @@ {
-  "name": "off-campus-housing-tracker",
+  "name": "record-platform",
   "private": true,
   "version": "0.4.0",
   "workspaces": [
@@ -49,7 +49,7 @@     "kafka-ssl": "bash scripts/kafka-ssl-from-dev-root.sh",
     "verify-tls": "bash scripts/verify-caddy-strict-tls.sh",
     "preflight-and-suites": "bash scripts/run-preflight-scale-and-all-suites.sh",
-    "setup:full-stack": "bash scripts/setup-full-off-campus-housing-stack.sh",
+    "setup:full-stack": "bash scripts/setup-full-record-platform-stack.sh",
     "make:help": "make help",
     "demo": "make demo",
     "demo:network": "make demo-network",

--- a/scripts/create-kafka-event-topics-k8s.sh+++ b/scripts/create-kafka-event-topics-k8s.sh@@ -7,7 +7,7 @@ # Prereqs: Stable API tunnel (./scripts/colima-api-health.sh), pods kafka-0..2 Ready.
 #
 # Env:
-#   KAFKA_K8S_NS=off-campus-housing-tracker
+#   KAFKA_K8S_NS=record-platform
 #   KAFKA_K8S_POD=kafka-0
 #   KAFKA_K8S_REPLICATION_FACTOR=3
 #   ENV_PREFIX, PARTITIONS, OCH_KAFKA_TOPIC_SUFFIX — same as create-kafka-event-topics.sh
@@ -18,7 +18,7 @@ REPO_ROOT="${REPO_ROOT:-$(cd "$SCRIPT_DIR/.." && pwd)}"
 export PATH="$SCRIPT_DIR/shims:/opt/homebrew/bin:/usr/local/bin:${PATH:-}"
 
-NS="${KAFKA_K8S_NS:-off-campus-housing-tracker}"
+NS="${KAFKA_K8S_NS:-record-platform}"
 KPOD="${KAFKA_K8S_POD:-kafka-0}"
 RF="${KAFKA_K8S_REPLICATION_FACTOR:-3}"
 ENV_PREFIX="${ENV_PREFIX:-dev}"

--- a/scripts/lib/edge-test-url.sh+++ b/scripts/lib/edge-test-url.sh@@ -1,8 +1,8 @@ #!/usr/bin/env bash
-# Shared helpers: edge-only E2E / k6 (https://off-campus-housing.test), no port-forward / :4020.
+# Shared helpers: edge-only E2E / k6 (https://record.test), no port-forward / api-gateway:4000.
 # shellcheck shell=bash
 
-EDGE_TEST_DEFAULT_BASE="https://off-campus-housing.test"
+EDGE_TEST_DEFAULT_BASE="https://record.test"
 
 # Print normalized E2E API base to stdout; warnings on stderr. Rejects legacy port-forward URLs.
 edge_normalize_e2e_api_base() {
@@ -12,8 +12,8 @@     printf '%s\n' "$EDGE_TEST_DEFAULT_BASE"
     return 0
   fi
-  if [[ "$raw" =~ ^http://127\.0\.0\.1:4020 ]] || [[ "$raw" =~ ^http://localhost:4020 ]]; then
-    echo "⚠️  Ignoring legacy E2E_API_BASE=$raw (port-forward / :4020 removed). Using $EDGE_TEST_DEFAULT_BASE" >&2
+  if [[ "$raw" =~ ^http://127\.0\.0\.1api-gateway:4000 ]] || [[ "$raw" =~ ^http://localhostapi-gateway:4000 ]]; then
+    echo "⚠️  Ignoring legacy E2E_API_BASE=$raw (port-forward / api-gateway:4000 removed). Using $EDGE_TEST_DEFAULT_BASE" >&2
     printf '%s\n' "$EDGE_TEST_DEFAULT_BASE"
     return 0
   fi
@@ -32,7 +32,7 @@     printf '%s\n' "$EDGE_TEST_DEFAULT_BASE"
     return 0
   fi
-  if [[ "$raw" =~ ^http://127\.0\.0\.1:4020 ]] || [[ "$raw" =~ ^http://localhost:4020 ]]; then
+  if [[ "$raw" =~ ^http://127\.0\.0\.1api-gateway:4000 ]] || [[ "$raw" =~ ^http://localhostapi-gateway:4000 ]]; then
     echo "⚠️  Ignoring legacy BASE_URL=$raw. Using $EDGE_TEST_DEFAULT_BASE" >&2
     printf '%s\n' "$EDGE_TEST_DEFAULT_BASE"
     return 0
@@ -50,7 +50,7 @@ 
 # Best-effort: first LoadBalancer IP from common ingress / caddy services (housing cluster).
 edge_hint_lb_ip_for_och() {
-  local ns="${HOUSING_NS:-off-campus-housing-tracker}"
+  local ns="${HOUSING_NS=record-platform:-record-platform}"
   local ip=""
   if command -v kubectl >/dev/null 2>&1; then
     ip="$(kubectl get svc -n ingress-nginx -o jsonpath='{range .items[?(@.spec.type=="LoadBalancer")]}{.status.loadBalancer.ingress[0].ip}{"\n"}{end}' 2>/dev/null | head -1 | tr -d '\r')"

--- a/scripts/run-playwright-e2e-preflight.sh+++ b/scripts/run-playwright-e2e-preflight.sh@@ -2,12 +2,12 @@ # Wait for the public edge (/api/readyz) and run Playwright E2E (strict TLS, hostname only).
 # Invokes scripts/webapp-playwright-strict-edge.sh → playwright test (webapp/e2e — multiple spec files).
 # Optional: E2E_SCREENSHOTS=1 ./scripts/webapp-playwright-strict-edge.sh e2e/ui-screenshots.spec.ts → webapp/e2e/screenshots/*.png
-# No kubectl port-forward; no http://127.0.0.1:4020 — legacy E2E_API_BASE values are ignored.
+# No kubectl port-forward; no http://127.0.0.1api-gateway:4000 — legacy E2E_API_BASE values are ignored.
 # PLAYWRIGHT_EDGE_RECOVERY_STABLE_SEC (default 30) acts as a cluster/edge idle-stability gate before tests run.
 #
 # Usage: ./scripts/run-playwright-e2e-preflight.sh
 #   SKIP_PLAYWRIGHT_E2E=1  — exit 0 immediately
-#   E2E_API_BASE           — must be https (default https://off-campus-housing.test)
+#   E2E_API_BASE           — must be https (default https://record.test)
 #   NODE_EXTRA_CA_CERTS    — default REPO_ROOT/certs/dev-root.pem (for curl --cacert + Node TLS)
 #   PLAYWRIGHT_VERTICAL_STRICT / PLAYWRIGHT_STRICT_HTTP3 — set by run-preflight-scale-and-all-suites.sh by default
 #     (PREFLIGHT_PLAYWRIGHT_STRICT_HTTP3=1) for CI parity with webapp `test:e2e:strict-verticals-and-integrity`.

--- a/scripts/verify-kafka-event-topic-partitions.sh+++ b/scripts/verify-kafka-event-topic-partitions.sh@@ -33,7 +33,7 @@ 
 if [[ "${KAFKA_PARTITION_VERIFY_TARGET:-}" == "k8s" ]]; then
   command -v kubectl >/dev/null 2>&1 || fail "kubectl required for KAFKA_PARTITION_VERIFY_TARGET=k8s"
-  _ns="${KAFKA_K8S_NS:-off-campus-housing-tracker}"
+  _ns="${KAFKA_K8S_NS:-record-platform}"
   _pod="${KAFKA_K8S_POD:-kafka-0}"
   kubectl get pod "$_pod" -n "$_ns" --request-timeout=20s >/dev/null 2>&1 || fail "Pod $_pod not in $_ns"
   _inner_props='TS_PASS=$(cat /etc/kafka/secrets/kafka.truststore-password)

--- a/scripts/webapp-playwright-strict-edge.sh+++ b/scripts/webapp-playwright-strict-edge.sh@@ -4,7 +4,7 @@ # Usage (from repo root):
 #   ./scripts/webapp-playwright-strict-edge.sh
 # Env:
-#   E2E_API_BASE   — https only (default https://off-campus-housing.test); :4020 / http localhost rejected
+#   E2E_API_BASE   — https only (default https://record.test); api-gateway:4000 / http localhost rejected
 #   NODE_EXTRA_CA_CERTS — default REPO_ROOT/certs/dev-root.pem
 set -euo pipefail
 ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

--- a/services/api-gateway/src/server.ts+++ b/services/api-gateway/src/server.ts@@ -49,23 +49,23 @@ });
 
 // Housing gRPC targets (README ports)
-const AUTH_GRPC_TARGET = process.env.AUTH_GRPC_TARGET || "auth-service.off-campus-housing-tracker.svc.cluster.local:50061";
+const AUTH_GRPC_TARGET = process.env.AUTH_GRPC_TARGET || "auth-service.record-platform.svc.cluster.local:50061";
 const authGrpcClient = createAuthClient(AUTH_GRPC_TARGET);
 
 /** K8s readiness: false until auth gRPC Health/Check succeeds (liveness uses /healthz only). */
 let authUpstreamReady = false;
 
 // HTTP base URLs for housing services (README ports)
-const AUTH_HTTP = process.env.AUTH_HTTP || "http://auth-service.off-campus-housing-tracker.svc.cluster.local:4011";
-const LISTINGS_HTTP = process.env.LISTINGS_HTTP || "http://listings-service.off-campus-housing-tracker.svc.cluster.local:4012";
-const BOOKING_HTTP = process.env.BOOKING_HTTP || "http://booking-service.off-campus-housing-tracker.svc.cluster.local:4013";
-const MESSAGING_HTTP = process.env.MESSAGING_HTTP || "http://messaging-service.off-campus-housing-tracker.svc.cluster.local:4014";
-const TRUST_HTTP = process.env.TRUST_HTTP || "http://trust-service.off-campus-housing-tracker.svc.cluster.local:4016";
-const ANALYTICS_HTTP = process.env.ANALYTICS_HTTP || "http://analytics-service.off-campus-housing-tracker.svc.cluster.local:4017";
+const AUTH_HTTP = process.env.AUTH_HTTP || "http://auth-service.record-platform.svc.cluster.local:4011";
+const LISTINGS_HTTP = process.env.LISTINGS_HTTP || "http://listings-service.record-platform.svc.cluster.local:4012";
+const BOOKING_HTTP = process.env.BOOKING_HTTP || "http://booking-service.record-platform.svc.cluster.local:4013";
+const MESSAGING_HTTP = process.env.MESSAGING_HTTP || "http://messaging-service.record-platform.svc.cluster.local:4014";
+const TRUST_HTTP = process.env.TRUST_HTTP || "http://trust-service.record-platform.svc.cluster.local:4016";
+const ANALYTICS_HTTP = process.env.ANALYTICS_HTTP || "http://analytics-service.record-platform.svc.cluster.local:4017";
 /** HTTP upstream for /media/* and /api/media/* (reverse proxy). Required: gateway does not map these paths to gRPC MediaService. See ENGINEERING.md § Service Communication Patterns → MEDIA_HTTP. */
-const MEDIA_HTTP = process.env.MEDIA_HTTP || "http://media-service.off-campus-housing-tracker.svc.cluster.local:4018";
+const MEDIA_HTTP = process.env.MEDIA_HTTP || "http://media-service.record-platform.svc.cluster.local:4018";
 const NOTIFICATION_HTTP =
-  process.env.NOTIFICATION_HTTP || "http://notification-service.off-campus-housing-tracker.svc.cluster.local:4015";
+  process.env.NOTIFICATION_HTTP || "http://notification-service.record-platform.svc.cluster.local:4015";
 
 type AuthedRequest = Request & { user?: { sub?: string; email?: string; jti?: string } };
 type GatewayRequest = Request & { traceId?: string };
@@ -290,8 +290,8 @@     origin: [
       /^http:\/\/localhost(:\d+)?$/,
       /^http:\/\/127\.0\.0\.1(:\d+)?$/,
-      /^https:\/\/off-campus-housing\.local(:\d+)?$/,
-      /^https:\/\/off-campus-housing\.test(:\d+)?$/,
+      /^https:\/\/record-platform\.local(:\d+)?$/,
+      /^https:\/\/record-platform\.test(:\d+)?$/,
     ],
     credentials: true,
     methods: ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],

--- a/services/auth-service/src/server.ts+++ b/services/auth-service/src/server.ts@@ -957,7 +957,7 @@   <h2>8. Contact Us</h2>
   <p>If you have any questions about this Privacy Policy or our data practices, please contact us:</p>
   <ul>
-    <li><strong>Email:</strong> support@off-campus-housing-tracker.local</li>
+    <li><strong>Email:</strong> support@record-platform.local</li>
     <li><strong>Platform:</strong> Off-Campus-Housing-Tracker</li>
   </ul>
 </body>
@@ -1020,7 +1020,7 @@   <p>We reserve the right to modify these Terms at any time. Material changes will be notified at least 30 days in advance.</p>
 
   <h2>11. Contact Information</h2>
-  <p>If you have questions about these Terms, please contact us at support@off-campus-housing-tracker.local</p>
+  <p>If you have questions about these Terms, please contact us at support@record-platform.local</p>
 </body>
 </html>
   `);

--- a/services/booking-service/vitest.integration.kafka-env.ts+++ b/services/booking-service/vitest.integration.kafka-env.ts@@ -20,7 +20,7 @@ 
 /** Same three-seed bootstrap as infra/k8s/base/config/app-config.yaml (in-cluster DNS + TLS :9093). */
 export const BOOKING_IT_DEFAULT_CLUSTER_BOOTSTRAP_SSL =
-  "kafka-0.kafka.off-campus-housing-tracker.svc.cluster.local:9093,kafka-1.kafka.off-campus-housing-tracker.svc.cluster.local:9093,kafka-2.kafka.off-campus-housing-tracker.svc.cluster.local:9093";
+  "kafka-0.kafka.record-platform.svc.cluster.local:9093,kafka-1.kafka.record-platform.svc.cluster.local:9093,kafka-2.kafka.record-platform.svc.cluster.local:9093";
 
 export function applyBookingIntegrationKafkaBrokerEnv(): void {
   applyVitestClusterKafkaBrokerEnv(repoRoot);

--- a/services/common/src/grpc-clients.ts+++ b/services/common/src/grpc-clients.ts@@ -73,7 +73,7 @@   }
   const explicit = process.env.GRPC_SERVER_NAME || process.env.TLS_SERVER_NAME;
   if (explicit) return explicit;
-  return "off-campus-housing.test";
+  return "record.test";
 }
 
 function createClientWithOptions(ServiceClass: any, address: string, credentials: grpc.ChannelCredentials) {
@@ -87,7 +87,7 @@ }
 
 // Housing ports per README: 50061 auth, 50062 listings, 50063 booking, 50064 messaging, 50066 trust, 50067 analytics, 50068 media
-export function createAuthClient(address: string = "auth-service.off-campus-housing-tracker.svc.cluster.local:50061") {
+export function createAuthClient(address: string = "auth-service.record-platform.svc.cluster.local:50061") {
   const AuthService = authProto.auth.AuthService;
   return createClientWithOptions(AuthService, address, buildCredentials());
 }
@@ -114,7 +114,7 @@  * Fails if TLS/SNI/CA/client cert wrong or auth reports NOT_SERVING (e.g. DB down).
  */
 export function verifyAuthGrpcUpstream(
-  address: string = process.env.AUTH_GRPC_TARGET || "auth-service.off-campus-housing-tracker.svc.cluster.local:50061",
+  address: string = process.env.AUTH_GRPC_TARGET || "auth-service.record-platform.svc.cluster.local:50061",
   serviceName: string = process.env.AUTH_GRPC_HEALTH_SERVICE || "auth.AuthService",
   timeoutMs: number = Number(process.env.AUTH_UPSTREAM_VERIFY_TIMEOUT_MS || "15000")
 ): Promise<void> {
@@ -159,7 +159,7 @@  * AUTH_UPSTREAM_VERIFY_INITIAL_BACKOFF_MS (1000), AUTH_UPSTREAM_VERIFY_MAX_BACKOFF_MS (8000).
  */
 export async function verifyAuthGrpcUpstreamWithRetry(
-  address: string = process.env.AUTH_GRPC_TARGET || "auth-service.off-campus-housing-tracker.svc.cluster.local:50061",
+  address: string = process.env.AUTH_GRPC_TARGET || "auth-service.record-platform.svc.cluster.local:50061",
   serviceName: string = process.env.AUTH_GRPC_HEALTH_SERVICE || "auth.AuthService",
   opts?: {
     totalBudgetMs?: number;
@@ -206,32 +206,32 @@   throw new Error(String(lastErr ?? "auth upstream verify failed"));
 }
 
-export function createListingsClient(address: string = "listings-service.off-campus-housing-tracker.svc.cluster.local:50062") {
+export function createListingsClient(address: string = "listings-service.record-platform.svc.cluster.local:50062") {
   const ListingsService = listingsProto.listings.ListingsService;
   return createClientWithOptions(ListingsService, address, buildCredentials());
 }
 
-export function createBookingClient(address: string = "booking-service.off-campus-housing-tracker.svc.cluster.local:50063") {
+export function createBookingClient(address: string = "booking-service.record-platform.svc.cluster.local:50063") {
   const BookingService = bookingProto.booking.BookingService;
   return createClientWithOptions(BookingService, address, buildCredentials());
 }
 
-export function createMessagingClient(address: string = "messaging-service.off-campus-housing-tracker.svc.cluster.local:50064") {
+export function createMessagingClient(address: string = "messaging-service.record-platform.svc.cluster.local:50064") {
   const MessagingService = messagingProto.messaging.v1.MessagingService;
   return createClientWithOptions(MessagingService, address, buildCredentials());
 }
 
-export function createTrustClient(address: string = "trust-service.off-campus-housing-tracker.svc.cluster.local:50066") {
+export function createTrustClient(address: string = "trust-service.record-platform.svc.cluster.local:50066") {
   const TrustService = trustProto.trust.TrustService;
   return createClientWithOptions(TrustService, address, buildCredentials());
 }
 
-export function createAnalyticsClient(address: string = "analytics-service.off-campus-housing-tracker.svc.cluster.local:50067") {
+export function createAnalyticsClient(address: string = "analytics-service.record-platform.svc.cluster.local:50067") {
   const AnalyticsService = analyticsProto.analytics.AnalyticsService;
   return createClientWithOptions(AnalyticsService, address, buildCredentials());
 }
 
-export function createMediaClient(address: string = "media-service.off-campus-housing-tracker.svc.cluster.local:50068") {
+export function createMediaClient(address: string = "media-service.record-platform.svc.cluster.local:50068") {
   const MediaService = mediaProto.media.MediaService;
   return createClientWithOptions(MediaService, address, buildCredentials());
 }

--- a/services/common/src/kafka-vitest-cluster.ts+++ b/services/common/src/kafka-vitest-cluster.ts@@ -5,7 +5,7 @@  * Env:
  *   - OCH_INTEGRATION_KAFKA_FROM_K8S_LB=1 — discover kafka-{0,1,2}-external LoadBalancer IPs (alias: BOOKING_IT_KAFKA_FROM_K8S_LB=1)
  *   - OCH_INTEGRATION_KAFKA_BROKERS / BOOKING_IT_KAFKA_BROKERS — optional multi-seed string copied to KAFKA_BROKER
- *   - OCH_INTEGRATION_K8S_NAMESPACE / BOOKING_IT_K8S_NAMESPACE / HOUSING_NS — kubectl namespace (default off-campus-housing-tracker)
+ *   - OCH_INTEGRATION_K8S_NAMESPACE / BOOKING_IT_K8S_NAMESPACE / HOUSING_NS=record-platform — kubectl namespace (default record-platform)
  */
 import { execFileSync } from "node:child_process";
 import { existsSync } from "node:fs";
@@ -130,8 +130,8 @@   return (
     process.env.OCH_INTEGRATION_K8S_NAMESPACE?.trim() ||
     process.env.BOOKING_IT_K8S_NAMESPACE?.trim() ||
-    process.env.HOUSING_NS?.trim() ||
-    "off-campus-housing-tracker"
+    process.env.HOUSING_NS=record-platform?.trim() ||
+    "record-platform"
   );
 }
 

--- a/services/common/src/kafka.ts+++ b/services/common/src/kafka.ts@@ -87,7 +87,7 @@   : [`kafka:${brokerPort}`];
 
 export const kafka = new Kafka({
-  clientId: process.env.KAFKA_CLIENT_ID || "off-campus-housing-tracker",
+  clientId: process.env.KAFKA_CLIENT_ID || "record-platform",
   brokers: kafkaBrokers,
   ssl: sslConfig,
   connectionTimeout: Number(process.env.KAFKAJS_CONNECTION_TIMEOUT_MS || "4000"),

--- a/services/common/src/redis.ts+++ b/services/common/src/redis.ts@@ -9,7 +9,7 @@     const isTest = process.env.NODE_ENV === 'test' || !!process.env.VITEST
     const defaultHost = isTest
       ? '127.0.0.1'
-      : 'redis-external.off-campus-housing-tracker.svc.cluster.local'
+      : 'redis-external.record-platform.svc.cluster.local'
     const host = process.env.REDIS_HOST || defaultHost
     const port = process.env.REDIS_PORT || '6380'
     const db = process.env.REDIS_DB || '0'

--- a/services/common/src/tracing.ts+++ b/services/common/src/tracing.ts@@ -6,7 +6,7 @@ import { PeriodicExportingMetricReader, ConsoleMetricExporter } from "@opentelemetry/sdk-metrics";
 
 const serviceName = process.env.SERVICE_NAME || "unknown-service";
-const otelEndpoint = process.env.OTEL_EXPORTER_OTLP_ENDPOINT || "http://otel-collector.off-campus-housing-tracker.svc.cluster.local:4318";
+const otelEndpoint = process.env.OTEL_EXPORTER_OTLP_ENDPOINT || "http://otel-collector.record-platform.svc.cluster.local:4318";
 
 export function initTracing() {
   const sdk = new NodeSDK({

--- a/services/cron-jobs/src/jobs.ts+++ b/services/cron-jobs/src/jobs.ts@@ -3,7 +3,7 @@ /**
  * Housing cron worker: periodic POST to notification-service internal heartbeat.
  * Set NOTIFICATION_HEARTBEAT_URL (in-cluster recommended), e.g.
- * http://notification-service.off-campus-housing-tracker.svc.cluster.local:4015/internal/cron/heartbeat
+ * http://notification-service.record-platform.svc.cluster.local:4015/internal/cron/heartbeat
  */
 async function notificationHeartbeat() {
   const url = (process.env.NOTIFICATION_HEARTBEAT_URL || "").trim()

--- a/services/listings-service/src/analytics-sync.ts+++ b/services/listings-service/src/analytics-sync.ts@@ -3,7 +3,7 @@  * immediately. Uses the same event_id as Kafka so analytics.processed_events dedupes.
  */
 const DEFAULT_ANALYTICS_HTTP =
-  "http://analytics-service.off-campus-housing-tracker.svc.cluster.local:4017";
+  "http://analytics-service.record-platform.svc.cluster.local:4017";
 
 export async function syncListingCreatedToAnalytics(input: {
   eventId: string;

--- a/services/messaging-service/src/lib/cache.ts+++ b/services/messaging-service/src/lib/cache.ts@@ -62,7 +62,7 @@           process.env.REDIS_HOST ||
           (process.env.NODE_ENV === 'test'
             ? '127.0.0.1'
-            : 'redis-external.off-campus-housing-tracker.svc.cluster.local'),
+            : 'redis-external.record-platform.svc.cluster.local'),
         port: Number(process.env.REDIS_PORT || 6380),
         ...common,
       })

--- a/services/transport-watchdog/src/index.ts+++ b/services/transport-watchdog/src/index.ts@@ -7,7 +7,7 @@ const REDIS_URL = process.env.REDIS_URL || "redis://redis:6379";
 const KEY = process.env.TRANSPORT_WATCHDOG_REDIS_KEY || "och:gw:watchdog_throttle";
 const GATEWAY_READY_URL =
-  process.env.TRANSPORT_WATCHDOG_GATEWAY_URL || "http://127.0.0.1:4020/readyz";
+  process.env.TRANSPORT_WATCHDOG_GATEWAY_URL || "http://127.0.0.1api-gateway:4000/readyz";
 const INTERVAL_MS = Math.max(5000, Number.parseInt(process.env.TRANSPORT_WATCHDOG_INTERVAL_MS ?? "15000", 10) || 15000);
 const KEY_TTL_SEC = Math.max(30, Number.parseInt(process.env.TRANSPORT_WATCHDOG_KEY_TTL_SEC ?? "120", 10) || 120);
 

--- a/webapp/e2e/analytics-api.spec.ts+++ b/webapp/e2e/analytics-api.spec.ts@@ -10,7 +10,7 @@   });
 
   test("GET daily-metrics returns JSON", async ({ request }) => {
-    test.skip(!(await apiGatewayHealthy(request)), "edge API not reachable — set E2E_API_BASE and ensure https://off-campus-housing.test (or override) resolves");
+    test.skip(!(await apiGatewayHealthy(request)), "edge API not reachable — set E2E_API_BASE and ensure https://record.test (or override) resolves");
     const base = e2eApiBase();
     const date = new Date().toISOString().slice(0, 10);
     const r = await request.get(`${base}/api/analytics/daily-metrics?date=${encodeURIComponent(date)}`);

--- a/webapp/e2e/helpers.ts+++ b/webapp/e2e/helpers.ts@@ -2,17 +2,17 @@ import { setTimeout as sleep } from "node:timers/promises";
 import { expect, type APIRequestContext, type Page } from "@playwright/test";
 
-const DEFAULT_E2E_EDGE = "https://off-campus-housing.test";
+const DEFAULT_E2E_EDGE = "https://record.test";
 
 function normalizeE2eApiBaseRaw(raw: string | undefined): string {
   if (!raw?.trim()) return DEFAULT_E2E_EDGE;
   const t = raw.trim();
-  if (/127\.0\.0\.1:4020|localhost:4020/i.test(t)) return DEFAULT_E2E_EDGE;
+  if (/127\.0\.0\.1api-gateway:4000|localhostapi-gateway:4000/i.test(t)) return DEFAULT_E2E_EDGE;
   if (t.startsWith("http://")) return DEFAULT_E2E_EDGE;
   return t.replace(/\/$/, "");
 }
 
-/** Public edge URL for API checks (Caddy → HAProxy → gateway). No port-forward / :4020. */
+/** Public edge URL for API checks (Caddy → HAProxy → gateway). No port-forward / api-gateway:4000. */
 export function e2eApiBase(): string {
   return normalizeE2eApiBaseRaw(process.env.E2E_API_BASE);
 }

--- a/webapp/lib/config.ts+++ b/webapp/lib/config.ts@@ -1,7 +1,7 @@ /**
  * API base for the housing api-gateway.
- * - If NEXT_PUBLIC_API_BASE is set: browser calls that origin (e.g. https://off-campus-housing.test).
- * - If unset: use same-origin `/api/...` and rely on next.config.mjs rewrites to API_GATEWAY_INTERNAL (default http://127.0.0.1:4020).
+ * - If NEXT_PUBLIC_API_BASE is set: browser calls that origin (e.g. https://record.test).
+ * - If unset: use same-origin `/api/...` and rely on next.config.mjs rewrites to API_GATEWAY_INTERNAL (default http://127.0.0.1api-gateway:4000).
  */
 export function getApiBase(): string {
   const b = process.env.NEXT_PUBLIC_API_BASE?.replace(/\/$/, "");

--- a/webapp/next.config.mjs+++ b/webapp/next.config.mjs@@ -1,5 +1,5 @@ /** Default in dev: proxy /api/* to api-gateway (avoids CORS). Set NEXT_PUBLIC_API_BASE to hit the edge URL from the browser instead. */
-const gatewayInternal = process.env.API_GATEWAY_INTERNAL || "http://127.0.0.1:4020";
+const gatewayInternal = process.env.API_GATEWAY_INTERNAL || "http://127.0.0.1api-gateway:4000";
 
 /** @type {import('next').NextConfig} */
 const nextConfig = {

--- a/webapp/playwright.config.ts+++ b/webapp/playwright.config.ts@@ -1,6 +1,6 @@ import { defineConfig, devices } from "@playwright/test";
 
-const DEFAULT_E2E = "https://off-campus-housing.test";
+const DEFAULT_E2E = "https://record.test";
 
 function stripTrailingSlash(u: string): string {
   return u.replace(/\/$/, "");
@@ -8,12 +8,12 @@ 
 /**
  * Edge-only E2E: same URL the browser uses for pages and API (Caddy → HAProxy → gateway).
- * Rejects legacy port-forward env (http://127.0.0.1:4020, etc.).
+ * Rejects legacy port-forward env (http://127.0.0.1api-gateway:4000, etc.).
  */
 function normalizeE2eApiBase(): string {
   const raw = process.env.E2E_API_BASE?.trim();
   if (!raw) return DEFAULT_E2E;
-  if (/127\.0\.0\.1:4020|localhost:4020/i.test(raw)) return DEFAULT_E2E;
+  if (/127\.0\.0\.1api-gateway:4000|localhostapi-gateway:4000/i.test(raw)) return DEFAULT_E2E;
   if (raw.startsWith("http://")) return DEFAULT_E2E;
   return stripTrailingSlash(raw);
 }

--- a/webapp/playwright.global-setup.ts+++ b/webapp/playwright.global-setup.ts@@ -10,7 +10,7 @@     return;
   }
 
-  const raw = process.env.E2E_API_BASE?.trim() || "https://off-campus-housing.test";
+  const raw = process.env.E2E_API_BASE?.trim() || "https://record.test";
   const base = raw.replace(/\/$/, "");
   const caPath = process.env.NODE_EXTRA_CA_CERTS?.trim();
   if (!caPath || !fs.existsSync(caPath)) {
