apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: record-platform
  labels: { app: api-gateway }
spec:
  selector: { app: api-gateway }
  ports:
    - name: http
      port: 4000
      targetPort: http
