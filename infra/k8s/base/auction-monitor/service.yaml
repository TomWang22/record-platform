apiVersion: v1
kind: Service
metadata:
  name: auction-monitor
  namespace: record-platform
  labels: { app: auction-monitor }
spec:
  selector: { app: auction-monitor }
  ports:
    - name: http
      port: 4010
      targetPort: http