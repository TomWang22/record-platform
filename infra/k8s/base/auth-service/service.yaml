apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: record-platform
  labels: { app: auth-service }
spec:
  selector: { app: auth-service }
  ports:
    - name: http
      port: 4001
      targetPort: 4001
    - name: grpc
      port: 50061
      targetPort: 50061
