apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: record-platform
  labels: { app: auth-service }
spec:
  selector: { app: auth-service }
  ports:
    - name: http
      port: 4001
      targetPort: http
    - name: grpc
      port: 50051
      targetPort: grpc
