apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# ServiceMonitor resources require Prometheus Operator CRDs
# These are optional - only include if Prometheus Operator is installed
# Commented out to avoid kustomize errors when CRDs are not installed
resources: []
#   - servicemonitors.yaml
