# infra/k8s/base/postgres/wal-archive-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pg-wal-archive
  namespace: record-platform
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: standard
  resources:
    requests:
      storage: 20Gi