apiVersion: v1
kind: Service
metadata:
  name: records-service
  namespace: record-platform
  labels: { app: records-service }
spec:
  selector: { app: records-service }
  ports:
    - name: http
      port: 4002
      targetPort: 4002
    - name: grpc
      port: 50051
      targetPort: 50051
