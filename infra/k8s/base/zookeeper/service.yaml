apiVersion: v1
kind: Service
metadata:
  name: zookeeper
  namespace: record-platform
  labels: { app: zookeeper }
spec:
  selector: { app: zookeeper }
  ports:
    - name: zookeeper
      port: 2181
      targetPort: zookeeper

