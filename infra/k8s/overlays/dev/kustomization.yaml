apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patchesStrategicMerge:
  - patches/replicas-api-gateway.yaml
  - patches/replicas-auth-service.yaml
  - patches/replicas-rest.yaml
