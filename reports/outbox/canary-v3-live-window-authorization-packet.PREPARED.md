# Canary-v3 live-window authorization packet (PREPARED)

**Status:** `PREPARED_NOT_AUTHORIZED`  
**Live window authorized:** `false`  
**Live capture acceptance ready:** `false`  
**Live capture armed for window:** `false`

Read-only capture implementations exist with fail-closed regression coverage for the acceptance blockers (command templates, independent equation terms, leader brackets, primary RecordMetadata, log cursor, explicit T0, observability readiness, Docker plane pin).

This packet does **not** authorize execution. `LIVE_CAPTURE_ACCEPTANCE_READY` remains `false` until an owner review promotes the capture path after the regression suite stays green and unset arm fields are filled.
